# Проверка доработки конструктора, 2026-09-21

| Требование владельца | Статус | Доказательство / следующий шаг |
| --- | --- | --- |
| Приглашение друга сохраняется и засчитывается | in progress | API тест гостевого заказа передаёт код; production SQL транзакция создала гостевой заказ, привязала его к аккаунту друга и проверила `buyer_referrals`, затем откатилась. Реальная ссылка → первый заказ → оплата и выдача → награда через браузер ещё не пройдены. |
| Создание магазина без лояльности; настройка позже | in progress | Кнопка пропуска и серверный null опубликованы; production SQL в откатываемой транзакции создал **новый** каталог еды без программы, после отката не осталось тестового магазина. Настройки существующего «Моё кафе» открываются. Нужен браузерный путь создания нового магазина. |
| Состав, вес, КБЖУ, добавки и комбо в первом товаре | in progress | Поля перенесены к первому шагу, схема и витрина обновлены. На мобильном production-экране существующего тестового блюда вес 200 г и 350 ккал сохранились, восстановились при повторном входе и были удалены; исходное `nutrition=null`. Первое создание с этими полями и комбо ещё не пройдено. |
| Понятные числовые поля и остаток 0→1 | verified | Кнопки количества, селекты и ₸ опубликованы. В отдельном «Моё кафе» браузер сохранил 0→1, каталог показал 1, затем 1→0 и каталог показал 0 без ошибки. Production SQL проверил запрет чужому пользователю и запись через `stock_movements`. |
| Подсказка по комбо, кнопки бренда/доставки/предпросмотра | verified | В опубликованном редакторе комбо отключено до появления другого товара и рядом доступно объяснение; AI Studio показывает отдельные кнопки доставки и предпросмотра, логотип открывается одним нажатием. Мобильный экран 390px просмотрен. |
| Быстрая загрузка и плавность | in progress | AI Studio First Load JS в production build уменьшился с 187 до 148 кБ; каталог и склад считают связи за O(P+V). Опубликованное демо 390/1440px прошло меню→корзину без overflow. Мобильное поле чата сжато после визуального обнаружения перекрытия; проверка повторного релиза и показатели реальных пользователей ещё нужны. |

Внешний блокер для текущих пунктов отсутствует. SMS OTP/маркетинговая рассылка остаются отдельной задачей с зависимостью от провайдера и Sender ID; здесь они не подменяются гостевым заказом.
