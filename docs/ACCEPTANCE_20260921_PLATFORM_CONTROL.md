# Управление платформой Dukenim — 2026-09-21

Владелец: Ерсат, аккаунт `yersat47@gmail.com` с ролью `superadmin`. Обычный аккаунт владельца магазина `ersatik@gmail.com` имеет отдельную область `/stores`. SMS-рассылка исключена из этого запроса.

| Требование | Статус | Доказательство / следующий шаг |
| --- | --- | --- |
| Вход суперадминистратора | verified | В production Auth подтверждены email, роль `superadmin`, провайдеры `email` и `google`; существующая Edge-сессия открыла защищённые `/root/accounts`, `/root/orders`, `/root/finance`, `/root/audit`. `/login` и `/forgot-password` существуют. Пароль из Auth не извлекается. |
| Раздел только для владельца платформы | verified | `/root/layout.tsx` требует `superadmin`; проверенный обычный владелец перенаправлен с `/root/management`. |
| Единый список аккаунтов и доступов | verified | `/root/accounts` под существующей superadmin-сессией показал 23 аккаунта, роли и магазины; на 390 px нет горизонтального переполнения. |
| Отзыв и восстановление доступа сотрудника | in progress | Сервисная SQL-функция проверяет суперадминистратора, точный email и ревизию, атомарно меняет `staff_access.active` и пишет аудит. Production rollback проверил revoke/restore, неверный email/роль и отсутствие доступа у `anon`/`authenticated`; экран открыт на production, но действующих сотрудников нет, поэтому реальный браузерный POST не проводился. |
| Блокировка аккаунтов, смена глобальных ролей, отзыв сессий | not started | Не добавлены небезопасные кнопки без гарантии отзыва действующего JWT и аудита. |
| Все заказы, состав, контакт, оплата, остатки | verified | `/root/orders` под superadmin показал 5 существующих заказов; открыта детальная карточка с составом, контактом, статусом оплаты и движением остатка. Поиск ограничен последними 500. |
| Отмены, возвраты и споры по всем магазинам | in progress | Добавлена ограниченная root-отмена незавершённого заказа только при `payment_status=pending`: номер и статус сверяются под блокировкой, штатные триггеры возвращают товар/сырьё и пересчитывают лояльность, аудит атомарен. Production rollback проверил отмену, неверный номер и запрет оплаченного заказа. В production-браузере форма видна у неоплаченного №5 и отсутствует у оплаченного №4; реальный POST не проводился. Возвраты денег и споры ещё не реализованы. |
| Состояние доставки, лояльности, историй и публикации | in progress | В браузере открыт root-обзор HYPESHOP: каталог, доставка, самовывоз, онлайн-оплата, лояльность и истории показаны. Root-управление публикацией выпущено; в браузере раскрыта форма с подтверждением адреса и причиной. Rollback проверил закрытие, отказ в открытии просроченного магазина и повторную публикацию после временного приведения к готовности. В реальной витрине ничего не меняли. Изменение доставки/лояльности/историй из root ещё не реализовано. |
| Остатки товара всех магазинов | in progress | Добавлена root-корректировка варианта через `stock_movements` с проверкой ожидаемого остатка и атомарным аудитом. Production rollback проверил stale/wrong-tenant отказ, движение и возврат исходного остатка. Production-браузер показал четыре варианта HYPESHOP и формы коррекции; реальный POST не выполнялся. Экран показывает до 1000 вариантов на магазине. |
| Подписки, тарифы и CRM-платежи | verified | `/root/finance` загрузил фактические записи базы. У старой тестовой подписки status `active`, но период истёк; исправлен счётчик и добавлена пометка истечения. Реальные списания/возвраты не проверены. |
| Внешние CRM | in progress | Ручной статус `connected` теперь требует активного соединения и успешной синхронизации. Для реального пилота нужны учётные данные и права конкретного клиента CRM. |
| Аудит и экспорт | verified | `/root/audit` показал существующие события; в Edge под superadmin началась загрузка CSV. Экспорт ограничен 10 000 событиями и не включает metadata/секреты. |
| Резервные копии, восстановление, оповещения | not started | Нужен отдельный инфраструктурный процесс и тестовое восстановление. Текущий CSV аудита не является резервной копией. |
| SMS-рассылка | externally blocked | Исключена из запроса владельцем; платформа не выдаёт её за работающую. |

Проверки: `npx tsc --noEmit`, `npm run build`, `npm test -- --run` (466 passed, 4 live-AI skipped); authenticated Edge smoke under superadmin and 390px overflow check. Root-релизы `130c05d`, `3b81d03`, `882b4e9`, `d0591bc` и `0efe3a8` опубликованы и Vercel Ready. Финансовая пометка `срок истёк`, защита кнопки отмены оплаченного заказа, формы публикации и остатков проверены в production-браузере. Azure не менялся. Следующее действие: продолжить оставшиеся внутренние инструменты и внешние финансовые/CRM-подключения.
