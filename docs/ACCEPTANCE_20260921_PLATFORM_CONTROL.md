# Управление платформой Dukenim — 2026-09-21

Владелец: Ерсат, аккаунт `yersat47@gmail.com` с ролью `superadmin`. Обычный аккаунт владельца магазина `ersatik@gmail.com` имеет отдельную область `/stores`. SMS-рассылка исключена из этого запроса.

| Требование | Статус | Доказательство / следующий шаг |
| --- | --- | --- |
| Вход суперадминистратора | in progress | В production Auth подтверждены email, роль `superadmin`, провайдеры `email` и `google`; браузерный вход под этим пользователем не выполнен. `/login` и `/forgot-password` существуют. Пароль из Auth не извлекается. |
| Раздел только для владельца платформы | verified | `/root/layout.tsx` требует `superadmin`; проверенный обычный владелец перенаправлен с `/root/management`. |
| Единый список аккаунтов и доступов | in progress | Добавлен `/root/accounts`, запросы Auth/профилей/членств/сотрудников защищены ролью; TypeScript/build проходят. Вход суперадминистратора в production-браузере нужен для сквозной проверки. |
| Блокировка аккаунтов, смена ролей, отзыв сессий | not started | Не добавлены небезопасные кнопки без гарантии отзыва действующего JWT и аудита. |
| Все заказы, состав, контакт, оплата, остатки | in progress | Добавлены `/root/orders` и `/root/orders/[id]`, охват последних 500, запросы к production-схеме проверены; браузерная проверка под root ещё нужна. |
| Отмены, возвраты и споры по всем магазинам | not started | Требуются безопасные транзакции с обратным движением товара и проверенным статусом платёжного провайдера. |
| Состояние доставки, лояльности, историй и публикации | in progress | Обзор добавлен в `/root/stores/[id]`; редактирование из root ещё не реализовано. |
| Подписки, тарифы и CRM-платежи | in progress | Добавлен `/root/finance` с базовыми статусами без ложного заявления о поступлении денег. Реальные списания/возвраты не проверены. |
| Внешние CRM | in progress | Ручной статус `connected` теперь требует активного соединения и успешной синхронизации. Для реального пилота нужны учётные данные и права конкретного клиента CRM. |
| Аудит и экспорт | in progress | Добавлены `/root/audit` и защищённый CSV-экспорт до 10 000 событий без metadata/секретов; авторизованный браузерный экспорт ещё не проверен. |
| Резервные копии, восстановление, оповещения | not started | Нужен отдельный инфраструктурный процесс и тестовое восстановление. Текущий CSV аудита не является резервной копией. |
| SMS-рассылка | externally blocked | Исключена из запроса владельцем; платформа не выдаёт её за работающую. |

Проверки: `npx tsc --noEmit`, `npm run build`, `npm test -- --run` (462 passed, 4 live-AI skipped). Azure не менялся. Следующее действие: опубликовать код, войти под `yersat47@gmail.com` и пройти root-экраны в браузере; затем продолжить работу над оставшимися внутренними инструментами и внешними финансовыми/CRM-подключениями.
