import {useCallback,useEffect,useState} from "react";
import {ActivityIndicator,Alert,Pressable,StyleSheet,Switch,Text,TextInput,View} from "react-native";
import {EditorScreen} from "@/components/editor-screen";
import {ui} from "@/components/app-shell";
import {useOwnerStore} from "@/lib/use-owner-store";
import {colors} from "@/lib/theme";
import {supabase} from "@/lib/supabase";
type Trigger="orders"|"spend"|"referral";type Reward="percent"|"fixed";
type Rule={id:string;trigger:Trigger;threshold:number;category:string;reward:Reward;label:string;value:number;minOrder:number;expiryDays:number;repeat:boolean;earnOnReward:boolean;giftVariantId:null};
const makeId=()=>`${Date.now().toString(16).padStart(8,"0").slice(-8)}-${Math.random().toString(16).slice(2,6).padEnd(4,"0")}-4${Math.random().toString(16).slice(2,5).padEnd(3,"0")}-a${Math.random().toString(16).slice(2,5).padEnd(3,"0")}-${Math.random().toString(16).slice(2,14).padEnd(12,"0")}`;
const fresh=():Rule=>({id:makeId(),trigger:"orders",threshold:6,category:"",reward:"percent",label:"Скидка постоянному гостю",value:10,minOrder:0,expiryDays:0,repeat:true,earnOnReward:false,giftVariantId:null});
const triggerLabels:Record<Trigger,string>={orders:"За количество заказов",spend:"За сумму покупок",referral:"За приглашённых друзей"};
const rewardLabels:Record<Reward,string>={percent:"Скидка, %",fixed:"Скидка, ₸"};
export default function Loyalty(){const{store}=useOwnerStore();const[name,setName]=useState("Клуб гостей"),[enabled,setEnabled]=useState(true),[terms,setTerms]=useState(""),[rules,setRules]=useState<Rule[]>([fresh()]),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false);const load=useCallback(async()=>{if(!store||!supabase)return;const[{data:program},{data:ruleRows}]=await Promise.all([supabase.from("loyalty_programs").select("name,enabled,terms").eq("tenant_id",store.id).maybeSingle(),supabase.from("loyalty_rules").select("config").eq("tenant_id",store.id).eq("active",true).order("created_at")]);if(program){setName(program.name);setEnabled(program.enabled);setTerms(program.terms??"");}if(ruleRows?.length)setRules(ruleRows.map(row=>row.config as unknown as Rule));setLoading(false);},[store]);useEffect(()=>{void load();},[load]);const patch=(index:number,value:Partial<Rule>)=>setRules(current=>current.map((rule,i)=>i===index?{...rule,...value}:rule));const save=async()=>{if(!store||!supabase)return;if(name.trim().length<2||rules.some(rule=>rule.threshold<1||rule.value<1||rule.label.trim().length<2))return Alert.alert("Проверьте условия","Название, порог и награда должны быть заполнены.");if(rules.some(rule=>rule.reward==="percent"&&rule.value>100))return Alert.alert("Проверьте процент","Скидка не может быть больше 100%.");setSaving(true);const program={name:name.trim(),enabled,terms:terms.trim(),rules};const{error}=await supabase.rpc("save_loyalty_program" as never,{p_tenant_id:store.id,p_program:program} as never);setSaving(false);Alert.alert(error?"Не удалось сохранить":"Программа сохранена",error?"Проверьте условия и повторите.":"Новые условия действуют на сайте и в приложении. Накопленный прогресс гостей сохранён.");};return <EditorScreen title="Лояльность" subtitle={store?.name}>{loading?<ActivityIndicator color={colors.navy}/>:store?.business_vertical!=="food"?<View style={ui.card}><Text style={ui.cardTitle}>Раздел для еды</Text><Text style={ui.subtitle}>Программы подарков и приглашений сейчас доступны кафе, ресторанам, донерным, кондитерским и кофейням.</Text></View>:<><Text style={ui.title}>Клуб гостей</Text><Text style={ui.subtitle}>Покупатель видит прогресс в «Моих заказах». Приглашение друга засчитывается после первого оплаченного заказа друга.</Text><View style={s.toggle}><View style={{flex:1}}><Text style={ui.cardTitle}>Программа включена</Text><Text style={ui.subtitle}>Начислять прогресс по новым оплаченным заказам</Text></View><Switch value={enabled} onValueChange={setEnabled} trackColor={{false:"#D7DEE3",true:colors.navy}}/></View><View style={ui.card}><Text style={ui.label}>Название карты</Text><TextInput style={ui.input} value={name} onChangeText={setName} maxLength={60}/><Text style={ui.label}>Общие условия · необязательно</Text><TextInput style={[ui.input,s.multiline]} value={terms} onChangeText={setTerms} multiline maxLength={1200} placeholder="Где и как действует программа"/></View>{rules.map((rule,index)=><View key={rule.id} style={ui.card}><View style={s.row}><Text style={ui.cardTitle}>Правило {index+1}</Text>{rules.length>1?<Pressable onPress={()=>setRules(current=>current.filter((_,i)=>i!==index))}><Text style={s.remove}>Удалить</Text></Pressable>:null}</View><Text style={ui.label}>За что награждаем</Text><View style={s.chips}>{(Object.keys(triggerLabels) as Trigger[]).map(value=><Pressable key={value} onPress={()=>patch(index,{trigger:value})} style={[s.chip,rule.trigger===value&&s.chipOn]}><Text style={[s.chipText,rule.trigger===value&&s.chipTextOn]}>{triggerLabels[value]}</Text></Pressable>)}</View><Text style={ui.label}>{rule.trigger==="spend"?"Сумма покупок, ₸":rule.trigger==="referral"?"Количество друзей":"Количество заказов"}</Text><TextInput style={ui.input} value={String(rule.threshold)} onChangeText={value=>patch(index,{threshold:Math.max(0,Number(value.replace(/\D/g,""))||0)})} keyboardType="number-pad"/><Text style={ui.label}>Награда</Text><View style={s.chips}>{(Object.keys(rewardLabels) as Reward[]).map(value=><Pressable key={value} onPress={()=>patch(index,{reward:value})} style={[s.chip,rule.reward===value&&s.chipOn]}><Text style={[s.chipText,rule.reward===value&&s.chipTextOn]}>{rewardLabels[value]}</Text></Pressable>)}</View><TextInput style={ui.input} value={rule.label} onChangeText={value=>patch(index,{label:value})} maxLength={100} placeholder="Например: Скидка постоянному гостю"/><TextInput style={ui.input} value={String(rule.value)} onChangeText={value=>patch(index,{value:Math.max(0,Number(value.replace(/\D/g,""))||0)})} keyboardType="number-pad" placeholder={rule.reward==="percent"?"Процент":"Сумма в ₸"}/></View>)}{rules.length<8?<Pressable onPress={()=>setRules(current=>[...current,fresh()])} style={ui.outline}><Text style={ui.outlineText}>+ Добавить правило</Text></Pressable>:null}<Pressable disabled={saving} onPress={()=>void save()} style={ui.button}><Text style={ui.buttonText}>{saving?"Сохраняем…":"Сохранить программу"}</Text></Pressable></>}</EditorScreen>}
const s=StyleSheet.create({toggle:{flexDirection:"row",alignItems:"center",gap:12,borderRadius:20,borderWidth:1,borderColor:colors.line,padding:17},multiline:{minHeight:96,textAlignVertical:"top",paddingTop:14},row:{flexDirection:"row",alignItems:"center",justifyContent:"space-between"},remove:{fontSize:12,fontWeight:"900",color:colors.danger},chips:{flexDirection:"row",flexWrap:"wrap",gap:7},chip:{borderRadius:14,borderWidth:1,borderColor:colors.line,paddingHorizontal:11,paddingVertical:9},chipOn:{backgroundColor:colors.navy,borderColor:colors.navy},chipText:{fontSize:11,fontWeight:"800",color:colors.muted},chipTextOn:{color:"white"}});
