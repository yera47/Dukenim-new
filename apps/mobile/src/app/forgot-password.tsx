import { useState } from "react";
import { ActivityIndicator, Alert, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { router } from "expo-router";
import { SafeAreaView } from "react-native-safe-area-context";
import { colors } from "@/lib/theme";
import { supabase } from "@/lib/supabase";
export default function ForgotPassword(){const[email,setEmail]=useState("");const[pending,setPending]=useState(false);const send=async()=>{if(!supabase||!email.includes("@")){Alert.alert("Проверьте email");return;}setPending(true);const{error}=await supabase.auth.resetPasswordForEmail(email.trim().toLowerCase(),{redirectTo:"dukenim://auth-callback"});setPending(false);if(error)Alert.alert("Не удалось отправить письмо","Попробуйте ещё раз.");else Alert.alert("Проверьте почту","Откройте ссылку восстановления на этом iPhone.");};return <SafeAreaView style={s.page}><Pressable onPress={()=>router.back()}><Text style={s.back}>← Назад</Text></Pressable><View style={s.center}><Text style={s.title}>Восстановить доступ</Text><Text style={s.copy}>Отправим защищённую ссылку на email вашего аккаунта.</Text><TextInput value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none" autoComplete="email" placeholder="Email" style={s.input}/><Pressable disabled={pending} onPress={()=>void send()} style={s.button}>{pending?<ActivityIndicator color="white"/>:<Text style={s.buttonText}>Отправить ссылку</Text>}</Pressable></View></SafeAreaView>}
const s=StyleSheet.create({page:{flex:1,backgroundColor:"white",padding:24},back:{color:colors.navy,fontWeight:"800"},center:{flex:1,justifyContent:"center",gap:16},title:{fontSize:34,fontWeight:"900",color:colors.ink},copy:{color:colors.muted,fontSize:15,lineHeight:22},input:{minHeight:54,borderWidth:1,borderColor:colors.line,borderRadius:14,paddingHorizontal:14,fontSize:16},button:{minHeight:54,borderRadius:14,backgroundColor:colors.navy,alignItems:"center",justifyContent:"center"},buttonText:{color:"white",fontWeight:"900"}});
