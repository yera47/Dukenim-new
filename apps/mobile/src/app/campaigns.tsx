import {useCallback,useEffect,useState} from "react";
import {ActivityIndicator,Alert,Pressable,StyleSheet,Text,TextInput,View} from "react-native";
import {EditorScreen} from "@/components/editor-screen";
import {ui} from "@/components/app-shell";
import {useOwnerStore} from "@/lib/use-owner-store";
import {colors} from "@/lib/theme";
import {supabase} from "@/lib/supabase";
type Campaign={id:string;title:string;eyebrow:string|null;body:string|null;cta_label:string;status:"draft"|"published"|"archived";image_url:string|null};
export default function Campaigns(){const{store}=useOwnerStore();const[rows,setRows]=useState<Campaign[]>([]),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[title,setTitle]=useState(""),[eyebrow,setEyebrow]=useState(""),[body,setBody]=useState("");const load=useCallback(async()=>{if(!store||!supabase)return;setLoading(true);const{data,error}=await supabase.from("storefront_campaigns").select("id,title,eyebrow,body,cta_label,status,image_url").eq("tenant_id",store.id).order("created_at",{ascending:false});setLoading(false);if(error)Alert.alert("Не удалось загрузить акции");else setRows((data??[]) as Campaign[]);},[store]);useEffect(()=>{void load();},[load]);const create=async()=>{if(!store||!supabase||title.trim().length<2)return Alert.alert("Добавьте заголовок","Минимум два символа.");setSaving(true);const{error}=await supabase.from("storefront_campaigns").insert({tenant_id:store.id,title:title.trim(),eyebrow:eyebrow.trim()||null,body:body.trim()||null,cta_label:"Смотреть",cta_href:"#catalog",status:"draft"});setSaving(false);if(error)Alert.alert("Акция не сохранена","Проверьте тариф и данные.");else{setTitle("");setEyebrow("");setBody("");await load();}};const setStatus=async(row:Campaign,status:Campaign["status"])=>{if(!supabase)return;const{error}=await supabase.from("storefront_campaigns").update({status,updated_at:new Date().toISOString()}).eq("id",row.id);if(error)Alert.alert("Статус не изменён");else await load();};return <EditorScreen title="Акции" subtitle={store?.name}><Text style={ui.title}>Кампании</Text><Text style={ui.subtitle}>Черновики видите только вы. После публикации акция появляется на витрине и остаётся общей с веб-кабинетом.</Text><View style={ui.card}><Text style={ui.cardTitle}>Новая акция</Text><TextInput style={ui.input} value={title} onChangeText={setTitle} maxLength={90} placeholder="Например: Комбо недели"/><TextInput style={ui.input} value={eyebrow} onChangeText={setEyebrow} maxLength={40} placeholder="Надзаголовок · необязательно"/><TextInput style={[ui.input,s.multiline]} value={body} onChangeText={setBody} multiline maxLength={300} placeholder="Коротко объясните предложение"/><Pressable disabled={saving} onPress={()=>void create()} style={ui.button}><Text style={ui.buttonText}>{saving?"Сохраняем…":"Сохранить черновик"}</Text></Pressable></View>{loading?<ActivityIndicator color={colors.navy}/>:rows.map(row=><View key={row.id} style={ui.card}><Text style={s.eyebrow}>{row.eyebrow||"АКЦИЯ"}</Text><Text style={ui.cardTitle}>{row.title}</Text>{row.body?<Text style={ui.subtitle}>{row.body}</Text>:null}<Text style={s.status}>{row.status==="published"?"Опубликована":row.status==="archived"?"В архиве":"Черновик"}</Text><View style={s.actions}>{row.status!=="published"?<Pressable onPress={()=>void setStatus(row,"published")} style={s.smallPrimary}><Text style={s.primaryText}>Опубликовать</Text></Pressable>:<Pressable onPress={()=>void setStatus(row,"draft")} style={ui.outline}><Text style={ui.outlineText}>Снять с витрины</Text></Pressable>}<Pressable onPress={()=>void setStatus(row,"archived")} style={s.small}><Text style={s.smallText}>В архив</Text></Pressable></View></View>)}{!loading&&!rows.length?<Text style={ui.subtitle}>Акций пока нет.</Text>:null}</EditorScreen>}
const s=StyleSheet.create({multiline:{minHeight:92,textAlignVertical:"top",paddingTop:14},eyebrow:{fontSize:10,fontWeight:"900",letterSpacing:1.3,color:colors.navy},status:{alignSelf:"flex-start",paddingHorizontal:10,paddingVertical:6,borderRadius:10,backgroundColor:colors.navySoft,color:colors.navyDark,fontSize:11,fontWeight:"900"},actions:{flexDirection:"row",flexWrap:"wrap",gap:8},smallPrimary:{minHeight:46,borderRadius:13,backgroundColor:colors.navy,paddingHorizontal:15,alignItems:"center",justifyContent:"center"},primaryText:{color:"white",fontWeight:"900"},small:{minHeight:46,borderRadius:13,borderWidth:1,borderColor:colors.line,paddingHorizontal:15,alignItems:"center",justifyContent:"center"},smallText:{color:colors.muted,fontWeight:"800"}});
