# Dukenim — current project state

- 2026-09-24 staff security and native follow-up: commit `f24a20c` is Vercel Production Ready (`dpl_4ToSgt3JxbUs8P1Ycj6evujXf79D`) and its database migrations are applied. The rollback-only production staff regression passed; an authenticated owner completed onboarding and opened the redesigned production team page. The mobile push token cache now uses native AsyncStorage, and a privacy-minimal iOS orders widget is configured. The approved D silhouette is unchanged and blue is limited to the small inner door threshold. EAS detected the new widget target but Apple still needs a Bundle ID and provisioning profile for `kz.dukenim.app.widgets`; the live invitation mutation and Apple credential creation are paused at mandatory UI confirmation boundaries. Evidence: `docs/ACCEPTANCE_20260924_STAFF_SECURITY.md` and `docs/ACCEPTANCE_20260923_IOS_OWNER_APP.md`.

- 2026-09-23 logo correction and iOS build 7: replaced the independently drawn mobile/social SVG with the exact alpha geometry from the approved brandbook PNG. The D remains dark on light surfaces or white on dark; only the exact lower threshold and wordmark dot use blue `#6FB9E8`. Pixel comparison confirms 0 alpha-mask mismatches across 41,748 source pixels. The shared web logo, compact native in-app header, iOS icon, splash and Android adaptive foreground use the corrected assets without changing the surrounding form or visible header size. Mobile TypeScript/lint, Expo Doctor 21/21 and iOS export pass; the 79-page web production build passes and serves both light/reversed logo assets with HTTP 200. Final signed App Store build `d8f33f37-081d-4f99-b37d-fe92462218e8` finished successfully as Dukenim `1.0.0 (7)` with IPA `https://expo.dev/artifacts/eas/I8FaJYpd2u4ex6e0RNMiUN5lR1JTIcF7NyzSBPBaOMk.ipa`. TestFlight still needs one-time Apple authentication/API key; Azure unchanged.

- 2026-09-23 iOS release-preparation follow-up: the owner app now uses the approved Dukenim social mark for its 1024 px icon, supports foreground notification presentation and Expo push-token rotation, and exposes registration/recovery, web cabinet, support, privacy and offer links. EAS Metadata validates for RU/en-US, and a manual EAS workflow validates for approval-gated submission of the latest production build to TestFlight. Mobile TypeScript/lint, Expo Doctor 21/21, iOS export, root TypeScript and the 79-page web build pass; local `/support` returns 200. Signed App Store build `d6c54c96-597a-43e0-b3f5-e8246b49eacd` finished successfully as Dukenim `1.0.0 (4)` and produced an IPA. TestFlight still needs one-time Apple authentication or an App Store Connect API key, and device acceptance needs a physical iPhone. Azure unchanged. See `docs/ACCEPTANCE_20260923_IOS_OWNER_APP.md`.

- 2026-09-23 AI Studio/publication/staff production acceptance: commits `9d71e42`, `56c1805` and `5ef387a` are deployed on `www.dukenim.kz` (`dpl_BcXFxhkPUTJVDvdtoAwizNooSdDP`, Ready). The supplied AI Studio layout is now a bounded light chat workspace with three contextual actions, fixed composer, full-width light mobile navigation and a pinned publication dock; the nested storefront and long settings stack are gone. A disposable production owner completed onboarding, created a stocked product and published its public storefront. A separate employee browser used a generated query-token invitation to create a confirmed account, accept manager access and open `/staff`; the owner then saw the member. Exact cleanup returned 0 QA users, tenants, staff rows, invitations and Storage objects. Production public audit remains 62/62 HTTP 200 with zero page errors/overflow. 484 tests pass with four intentional live-AI skips; strict TypeScript and the 78-page build pass. Polar live payment/webhook, physical-iPhone measurement and deferred SMS still require external setup. Azure unchanged. See `docs/ACCEPTANCE_20260923_AI_STUDIO_RELEASE.md`.

- 2026-09-23 full site/AI Studio audit: app commit `523dbcc` is contained in the current Vercel Production release on `www.dukenim.kz`. AI Studio now matches the owner reference with a single light workspace, three status-aware quick actions, fixed ChatGPT-style composer, advanced photo/PDF/camera/logo/banner/support actions under `+`, and a full-width light mobile navigation. The audit also fixed 390 px overflow in long legal headings and the cookies panel, respects reduced motion, and gives story previews explicit lazy image dimensions. Production browser opened 31 public routes at 390/1440 px (62 renders): all HTTP 200, zero page errors and zero overflow; average DOMContentLoaded 776/689 ms. A disposable production owner completed registration/onboarding and the finished-catalog AI Studio passed both viewports with zero document scroll; exact cleanup returned 0 QA users, 0 QA tenants and 0 QA Storage objects. 482 tests pass with four intentional live-AI skips; strict TypeScript and the 78-page build pass. Physical-phone measurement, approved legal identity details and live SMS/payment/CRM providers remain external blockers. Azure unchanged. See `docs/AUDIT_20260923_SITE_AI_STUDIO.md`.

- 2026-09-22 manual Kaspi confirmation follow-up: `902f409` is Vercel Production Ready and aliased to `www.dukenim.kz`. The external Kaspi link is explicitly a transition only: Dukenim creates the order first, My Orders shows the exact amount, and the buyer enters it in Kaspi manually. After independently checking the receipt, the owner now uses one action that atomically marks payment `paid` and a new order `confirmed`; unpaid fulfilment remains blocked. A fresh 390×844 production owner registered, uploaded a PNG through the redesigned AI Studio and saved/enabled a Kaspi transition link; direct database checks confirmed both the private Storage object and `tenant_settings`. Exact QA account/store/file cleanup returned zero QA remnants and preserved totals at 24 users / 21 tenants / 5 orders / 18 Storage objects. Production also has an owner-scoped Storage delete policy for brand materials. 482 tests pass with four live-AI skips; TypeScript, targeted ESLint and production build pass. No real money moved, physical iPhone was not used, SMS is still deferred and Azure was unchanged. See `docs/ACCEPTANCE_20260922_AI_STUDIO_KASPI.md`.

- 2026-09-22 AI Studio/Kaspi release: `c1882d3` is Vercel Production Ready and aliased to `www.dukenim.kz`. AI Studio now has a chat-first white shell in all catalog stages, three quick tasks and a bottom composer while preserving existing consultation and explicit save/apply flows. For merchants who enable manual Kaspi Pay, checkout presents that single remote payment choice; after order creation My Orders shows the exact goods amount, a link/invoice path and stages through owner payment confirmation and delivery/pickup. The buyer enters the amount in Kaspi manually; Dukenim does not auto-confirm receipt. Manual Yandex courier remains owner-ordered after price agreement, with no buyer-facing courier price. Pickup details include 2GIS/Yandex map links. 478 tests, TypeScript, targeted ESLint and production build passed. A fresh public food-demo checkout returned 200 and rendered, but authenticated mobile AI Studio, live Kaspi settlement and buyer-to-merchant production order remain unverified: the present merchant browser session redirects to onboarding and an older AI Studio tab contains stale deleted-store state. Azure unchanged. See `docs/ACCEPTANCE_20260922_AI_STUDIO_KASPI.md`.

- 2026-09-22 owner setup/brand release: `cc6e896` pushed to `main`; production Supabase migration `catalog_setup_manual_delivery_payment` applied. First-run catalog setup now offers the existing manual Yandex courier and manual remote Kaspi Pay, with owner-facing links on the catalog page to both later settings. A production transactional RPC probe confirmed provider/Kaspi persistence and rolled all probe changes back. The shared Dukenim wordmark again uses the original 23×25 silhouette with only the lower threshold blue; homepage and legal/onboarding headers in production render the new component. 478 tests passed (4 intentional live-AI skips), strict TypeScript and 78-route build pass; local 390 px homepage has no horizontal overflow. Authenticated browser completion of a fresh catalog with both choices is still pending; no existing store setting or order was changed. Azure unchanged. See `docs/ACCEPTANCE_20260922_OWNER_SETUP_BRAND.md`.

- 2026-09-22 sales launch recheck: `ee415f3` is Vercel Production Ready on `www.dukenim.kz`. Public examples now use built-in demos independent of the stores requested for deletion; unsupported service booking was removed from the homepage showcase, demo CTAs lead to registration, and the card-payment statement clarifies that buyer online payment requires separate connection. Production browser opened homepage, registration and food/other demos. Read-only production inventory remains 23 Auth users, 20 stores, 5 orders (4 locally marked paid, 304,000 KZT) and 17 Storage objects; 15 published trials are expired and the only published active store is synthetic `demo-shop`. No store has remote Kaspi or online payment enabled; no CRM integration is active. Supabase organization is Free, with no automatic daily database backup; Dashboard sign-in, independent DB+Storage export and restore rehearsal are pending, so the authorized bulk deletion was not performed. Live merchant order, real Polar payment, two-account chat and physical phone benchmarks remain unverified; SMS deferred. Azure unchanged. See `docs/ACCEPTANCE_20260922_SALES_READY.md`.

- 2026-09-22 production reset preflight: `779d58c` is Vercel Production Ready and aliased to `www.dukenim.kz`; the authenticated root browser shows paid-order totals as Dukenim status, explicitly not verified bank/Kaspi revenue. Read-only production inventory confirms 23 Auth users (22 potential removals), 20 stores, 5 orders (4 marked paid, 304,000 KZT), 17 Storage objects and one staff invitation. No legacy data was deleted: a verified independent database + Storage backup and restore rehearsal are not accessible yet. The owner was asked to sign in to Supabase Dashboard; `docs/RESET_AUDIT_20260922.md` and the tested read-only preflight query record the operation. TypeScript/build pass, Azure unchanged.

- 2026-09-22 AI context follow-up: `276f0e5` sends the saved catalog-builder merchant brief (bounded to 300 characters) into AI Studio consultation so a re-entered shop retains its business description. The brief is labelled unverified; no products are inferred. Four tenant-scoped context reads are concurrent. TypeScript, production build and 468 tests pass (4 live-AI skips). No Azure or database configuration changed. The separate live AI request predates this context addition; see audit for its verification boundary.

- 2026-09-22 demo navigation audit: `978862b` is Vercel Production Ready. The category deep-link from the fashion demo previously returned 404 for URL-encoded Cyrillic; the demo route now decodes and validates it. Production browser journeys pass at 390/1440 px for fashion category and food catalog, plus food combo/quantity/edit/reload/no-overflow checks. Strict TypeScript, build and 467 tests pass (4 live-AI skips). No Azure or database configuration changed.

- 2026-09-22 registration/food/AI release: `4d32118` is Vercel Production Ready. Food setup now offers seven editable business examples (restaurant, doner, bakery, confectionery, coffee, canteen, custom) while keeping two shared menu layouts. AI Studio explains actual capabilities and navigates to catalog, orders, loyalty and stories; its consultation prompt uses the food formats and answers the current task instead of restarting onboarding. A disposable 390px production-browser journey passed account creation → food/tariff → persistent catalog draft → delivery/payment-later/loyalty-skip → real product → separate publication → fresh-browser login and catalog recovery. One live AI request returned the food-stories route. All exact QA accounts/tenants were removed (0 remain). TypeScript, build and 466 tests pass, four live tests skipped. Azure configuration unchanged; one ordinary inference used. Broader platform gaps and advisor findings are recorded in `docs/AUDIT_20260922_REGISTRATION_AI.md`.

- 2026-09-21 root control release: `130c05d`, `3b81d03`, `882b4e9`, `d0591bc`, `0efe3a8` are Vercel Production Ready. `yersat47@gmail.com` is the confirmed superadmin; an authenticated browser opened root account/order/finance/audit/store pages. Root can read platform records, revoke staff access, cancel only unfinished unpaid orders, close/reopen a ready storefront and correct variant stock through stock movements, all with guarded server actions and audited database RPCs. Production SQL rollback verified mutations without permanently changing actual stores, orders or employees. TypeScript, build and 466 tests pass (4 live-AI skips). SMS mailing excluded. Full remaining work is tracked in `docs/ACCEPTANCE_20260921_PLATFORM_CONTROL.md`; Azure unchanged.

- 2026-09-21 root follow-up: `3b81d03` is Vercel Ready; `/root/finance` browser shows zero current subscriptions and marks the expired test row. `882b4e9` is pushed/deploying for root cancellation of unfinished, payment-pending orders only; production SQL rollback proved exact-number guard, paid-order rejection, atomic audit and unchanged real order afterward. Production enum drift was fixed in a second migration. TypeScript, build and 464 tests pass (4 live-AI skips). No actual order or employee access changed. Further paid refunds and disputes remain unfinished. Azure unchanged.

- 2026-09-21 platform-owner control update: `yersat47@gmail.com` is confirmed production `superadmin`; an existing authenticated Edge session opened the protected account, order, finance, audit and store overview routes. `130c05d` is Vercel Production Ready. `3b81d03` is pushed and deploying; its service-role-only audited staff revoke/restore RPC is already applied to production and passed rollback-only checks without altering any actual employee. Strict TypeScript, build and 463 tests pass (4 live-AI skips). Fresh login and second-deployment browser render remain to verify. Full capabilities and gaps are in `docs/ACCEPTANCE_20260921_PLATFORM_CONTROL.md`; financial refunds/reconciliation, global session controls, live CRM/payment connection and backups are unfinished. SMS mailing excluded by owner; Azure unchanged.

- 2026-09-21 platform control work in progress: production Auth confirmed `yersat47@gmail.com` has confirmed email, `superadmin` role and Google/email identities. Password is not retrievable; login and recovery routes exist. New root-only account, order, finance and audit screens and CSV export are in source; CRM status can no longer be manually called connected without a synced active connection. Root aggregate now says sum of recent orders, not collected revenue. TypeScript, build and 462 tests pass (4 live-AI skipped). Code publication and authenticated superadmin browser journey are still pending. SMS mailing excluded by owner. Azure unchanged. Full remaining acceptance is recorded in `docs/ACCEPTANCE_20260921_PLATFORM_CONTROL.md`.

- 2026-09-21 owner store reset: `262661b` is pushed and Vercel Production Ready. The owner's `ersatik@gmail.com` account now has zero stores after the authorized deletion of its only two owned tenants, `Dukenim shop` and `Моё кафе`; the Auth account and 19 unrelated platform stores remain. Both old storefront URLs return 404 and two deletion audit events persist. `/stores` now offers a self-service owner-scoped list, guarded deletion and creation from the existing account; `/root/management` lists existing platform controls and remaining gaps. Authenticated production browser showed the empty state, signed-in creation form and `/admin` redirect. New-store submission is reserved for the owner's own walkthrough; no new products or sales were added. The service-role-only SQL guards were tested in rollback; TypeScript, production build and 461 tests passed. Azure unchanged.

- 2026-09-21 platform-owner clarification: Ersat specified that the full-platform management map is for his superadmin cabinet, not seller/client cabinets. `f5eba19` removes it from `/stores` and rewrites `/root/management` around existing root controls and gaps. The route inherits superadmin-only protection from `/root/layout.tsx`. TypeScript and build passed; both `f5eba19` and the later decisions-only `0a8bb1d` Vercel Production deployments are Ready. The canonical `/stores` no longer shows the map, and the ordinary owner was redirected away from `/root/management`. Ordinary merchant self-service store deletion remains in `/stores`.

- 2026-09-21 iiko reply: the technology-partner lead confirmed Dukenim may independently build an integration using iiko's public API, but iiko will not onboard new partners or provide a sandbox/pilot service in 2026. Each pilot restaurant must independently register and obtain its own `apiLogin`; iiko says a dealer is probably unnecessary. No API login, venue account, credential, order or contract was created. Poster, r_keeper, Quick Resto and other outreach targets sent no substantive reply in this mailbox recheck; JOWI's `info@jowi.app` mailbox bounced.

- 2026-09-21 food builder/stock follow-up: `7577746`, mobile fix `2fb1069`, Studio link `624ccd6` and parallel-read release `aa7edf3` are pushed and Vercel Ready on `www.dukenim.kz`. Optional food loyalty, structured first-product nutrition/composition, clear combo help, stock 0→1 editing via owner-guarded stock movements, and clearer AI Studio actions are published. A production SQL rollback created a fresh food catalog with no loyalty and left zero fixture stores; another rollback saved and re-enabled a referral rule on a fresh catalog. A paid/completed first friend order yielded one inviter reward while a second order by the same friend gave no extra referral progress; rollback left the private store unpublished with zero orders/members/referrals and original stock. Authenticated browser in the private «Моё кафе» saved stock 0→1→0 without the generic section error and saved/reloaded/cleared weight and kcal; original nutrition remains null, stock zero and storefront private. At 390px the compact composer ends at y754, above navigation starting y761, with no horizontal overflow. The published direct Studio loyalty link opened the settings editor at 390px. AI Studio initial reads now overlap; canonical authenticated reload rendered correctly, but real-user latency impact is unmeasured. 461 tests, TypeScript and 78-route build pass. A fresh 390px production browser registered a disposable food store, skipped loyalty, saved its catalog and first product with weight/KБЖУ, removable ingredient and paid add-on, then added a referral rule in settings, reloaded, disabled and re-enabled it. Exact fixture cleanup returned zero tenants and users, leaving real stores unchanged. A second pair of disposable accounts then completed the live `?ref=` link → signed-in friend order → owner cash confirmation and `done` status → inviter card showing «Награда готова». Database confirmed one linked referral and a tagged `paid/done` order. Both fixture stores/accounts were deleted, leaving zero test orders/referrals. Final UI copy/layout release `2fef213` is Vercel Ready; strict TypeScript and 78-route build pass. Real-user speed remains unmeasured. SMS remains intentionally deferred, Azure unchanged.

- 2026-09-21 pickup setup follow-up: official Yandex Navigator `/navi` links are accepted alongside Yandex Maps links, while unsafe hosts/schemes remain rejected. Pickup/reservation hours now use scrollable 30-minute choices plus daily and weekday presets rather than manual time fields; validation points to the specific invalid address, hours or map link. Release `414fd2e` is Ready on `www.dukenim.kz` (`dpl_8kbq3mq97PhAp4PRNY84EpYecLwJ`); authenticated browser reopened the saved «Моё кафе» draft with the exact Navigator link, no validation error and an enabled Continue button. The new time choices are verified by component tests; a browser click through the step remains untested. 456 tests, strict TypeScript and production build pass.

- 2026-09-21 manual courier sales follow-up: guest checkout is implemented for the period before platform SMS activation, with required +7 contact phone, privacy/offer consent, a separate manual-courier price acknowledgement, browser-bound order history and no unverified reward redemption. Merchant order cards show buyer contact and full address, without a Yandex account prerequisite. SQL transaction smoke created a guest courier order with `delivery_cost=0`, `payment_status=pending`, provider snapshot `yandex` and guest history, then rolled it back; no test zone remained. Release `5979be8` is Ready on `www.dukenim.kz`; a fresh production browser checkout shows guest name, phone and privacy consent without SMS. 455 tests, strict TypeScript and 78-route production build passed before the pickup follow-up. No real buyer-to-manager browser order has been placed. Real OTP still needs the external provider account/key and approved sender.

- 2026-09-21 delivery/template follow-up: source through `ebe9a9c` is pushed and Ready on `www.dukenim.kz` (deployment `dpl_3fpM7SKufboAg5sQjgi2D3fozXDd`); a signed Supabase Auth → Mobizon OTP bridge is published but remains disabled without provider credentials. Owner-configured own or manual Yandex delivery zones use a full-screen Yandex acknowledgement, unpriced buyer checkout, direct manager Yandex link and order provider snapshots. Production database provider, snapshot and unpriced-Yandex constraints are applied; no existing zone was changed to Yandex. An authenticated owner saved an inactive Yandex zone with `cost=0` and no free threshold, then the temporary zone was removed. New food setup and public food-demo selection show two options: premium restaurant gallery and fast compact menu, sharing the same purchase functions; existing guided links remain functional. Production `/demo/food` browser view confirms two cards. Both food demos were visually checked at a genuine 390 px viewport with no horizontal overflow; a white-on-white premium CTA found during inspection was fixed and rechecked. Yandex map-link input accepts safe pasted URLs/embeds. 454 tests, TypeScript and production build pass. A buyer-to-manager Yandex order remains unverified because no public store has Yandex enabled and real phone OTP is unavailable. Live SMS needs a platform carrier account/key, approved Sender ID and Supabase hook/Phone Auth configuration. Azure unchanged.

- 2026-09-21 food stories and owner setup: `0c23af2` is pushed and Vercel deployment `dpl_DzgGzvnSp1waM2v4dDQ5Tvfos3yw` is Ready on `www.dukenim.kz`. Owners can upload/publish/edit/delete tenant-isolated photo or video stories with optional dish links; all food templates render them. Existing-account owners can switch stores. A separate, unpublished, empty food store `Моё кафе` is attached to the owner's existing Google account, with onboarding still at step 1 and a seven-day trial through 2026-09-28. Authenticated production browser checks verified switcher, photo/video upload, publication and real video playback; both disposable stories and media were removed, leaving zero stories/media. SMS settings are simpler and merchant-facing provider credentials are unnecessary, but live OTP and SMS remain externally blocked by the platform's missing SMS-provider account/key, Supabase Phone Auth provider configuration and approved Sender ID. No Azure changes.

- 2026-09-20 buyer phone/SMS and food operations: phone-first checkout and reservation, account-linked permanent history/loyalty, consented merchant SMS settings/campaign queue, automatic loyalty gifts, recipe/raw-material movements and root Sender ID moderation are implemented. Dark-blue platform accent and aligned logo are applied; food cart/customisation/combos remain intact. Four production migrations are applied, including a signed minutely SMS worker. 442 tests, strict TypeScript, 77-page build, and 390px food/mobile plus 390/1440 home visual checks pass. Commits `062e845` and `83a19ea` are pushed; deployment `dpl_BEYespANwRba1SG8LV7iXEXdLsA8` is Ready/current and canonical mobile smoke passes. Live OTP and outbound SMS remain externally blocked by missing Supabase phone-provider configuration, `MOBIZON_API_KEY`, and provider-approved Sender ID.

- 2026-09-19 food configuration and loyalty: implemented quantity/editable recipe cart, structured merchant ingredients/addons/real-product combos, permanent account order history, food loyalty builder and rule-based rewards. Seven production migrations applied; authenticated owner/buyer save/order/paid-completion/redeem/cancel/fresh-browser journey and 390/1440 visual checks pass. Source d874f0c is pushed; dpl_3KmhbgfdBsXgoNUFu9K1CrdaeBcp is Ready/promoted, with canonical-domain 390/1440 smoke passing. No Azure changes.


- 2026-09-19 food templates/CRM/Wraxa continuation: production release `e86511e` is Ready (`dpl_GrnzoScYwiNQEu4V2WE7ZAZ91ENV`) and adds two food paths backed by the shared storefront: compact quick menu with one-tap add (Frito flow) and large category-first discovery (Dodo flow), while preserving delivery/pickup gating and ASAP/scheduled checkout. Food copy now says menu/dishes, category navigation is not duplicated, and production 390px checks for both paths have no overflow. 427 tests, TypeScript and the 75-page production build pass. Wraxa account/project `Dukenim` was created through `ersatik@gmail.com`; Wraxa blocks generation/editor access behind its payment step, which was not entered. Polar private one-time product `Подключение CRM — Старт` (`4ea42e1a-2b45-46cb-b3d4-6aba0f6ec165`) is configured at 70,000 KZT, `POLAR_CRM_SETUP_PRODUCT_ID` is in Vercel Production, and redeployment `dpl_4Ej5nnic8VSK94pYvjWeSFbD1eMf` is Ready. Owner-approved `Dukenim shop` trial runs through 2026-09-26 10:51 UTC with an audit reason. Production browser acceptance now verifies builder save → logout → Google re-login → restored catalog → publish and public storefront. It also verifies staff invite → verified-email acceptance → restricted modules → permission change → revocation; final database state is inactive revision 3, no broad `tenant_users` membership, with invite/accept/two update audit events.

- 2026-09-19 food ordering/CRM follow-up: production release `be8a35e` is Ready (`dpl_5Kjpax9je7xXXdxqeLwpcJ2QagxA`) on `www.dukenim.kz`. It adds a food-first delivery/pickup gate, order timing (`asap` or scheduled 15 minutes–14 days) visible to buyer and merchant, a five-step Studio brief with HoReCa presets, and server-enforced CRM setup billing (Start 70,000 KZT after verified connection; Brand included). Migrations `20260919082323`, `20260919082953`, and `20260919083643` are applied. Rollback SQL verifies order timing, CRM amount/replay/tariff logic, staff invite/permissions/revocation and builder create/undo/publish. 423 tests, TypeScript, production build, 36 journeys at 390/1440 and focused 390px visual checks pass. Production browser smoke confirms the food choice and scheduled checkout as well as authenticated integrations and orders rendering. Actual CRM payment remains externally blocked until a Polar one-time product is configured as `POLAR_CRM_SETUP_PRODUCT_ID`. Authenticated employee UI and owner save/re-login/publish browser acceptance are not replaced by rollback tests. Azure unchanged.

- 2026-09-19 AI request cards: release `8b9ba91` is production Ready. Editable review/send/open flow, inline errors and database retry guard are implemented for payments/Kaspi/CRM/support. Authenticated payments-card send/reload/retry verified: one technical support request and one message persist. 421 tests, TypeScript, build74 and rollback SQL tenant/replay checks pass. Applied migration `20260919081102`. Owner-approved Amira trial extension expires 2026-09-26 08:06:42 UTC, audited, no payment. Full mobile audit, CRM70000 billing, staff and builder parent acceptance remain open. Azure unchanged; no new live generation.

- 2026-09-19 HoReCa follow-up: removed the daily CRM/POS automation at the owner's request. Gmail re-audit found no new credential or sandbox invitation; r_keeper auto-closed tickets #207879/#207963 after timeout. Sent and verified seven no-commitment partnership/API-pilot requests to Poster central/Kazakhstan, r_keeper central/Kazakhstan, iiko, Quick Resto Kazakhstan and JOWI. No payment, licence, contract, access credential, customer data or order was accepted/transmitted. Azure unchanged.

- 2026-09-11 workspace: eff54dc deployed; readable Studio actions, integration dialogs, AI help links, analytics date range, team directory, catalog campaigns and200/2000server product caps implemented. Production help/routing, modal/prefill, date report and team empty state checked.48488d4 pushed, build74passed, applied guarded empty-store deletion. No actual store deletion/payment. Billing and full authenticated mutation/mobile acceptance remain open.

- 2026-09-11 Business.Ru release: connector commit `3b1b25c` is deployed and Ready; production migration `20260910210037_business_ru_connection` is applied with RLS/browser-role restrictions preserved. The live credential transfer/read-only preflight is not complete: two Windows Computer Use attempts stopped before input because Edge's active URL could not be verified; the authenticated provider tab is currently in the non-automatable in-app browser. A post-check confirmed there is no `biznes_ru` connection/request row. No credential, customer, product or order was transmitted. Azure unchanged.

- 2026-09-11 recovery: release 4ed4814 deployed successfully. AI Studio restores the saved personal-design proposal on reopening, alongside persisted conversation. Authenticated production reload verified without regeneration or applying merchant changes; 8 focused tests, TypeScript and build pass. This does not verify the full apply/re-login/publication journey.

## Current acceptance — 2026-09-10

- Personal-assembly final release4faa7a4 is deployed (Vercel success), including building-stage handler, preserved first-product inputs and accurate waiting indicator.

- 19:20: production chat→AI proposal→visual preview confirmed. Fixed ability to request design before first product (5c71699 deployed); preserved hidden product form inputs. Personal proposal remains unapplied to Amira; no fabricated sales/products.403tests pass. Full owner apply/re-login/publication remains unverified.

- 19:12: personal layout source073489c deployed (Vercel success). AI chooses and saves typography/hero/density/grid/corners/image ratio alongside shades/copy. Applied migration and rollback creation/undo/staff tests pass; live Kimi design passed. Shared preview/public home renderer. Full owner browser acceptance and unrestricted page generation remain incomplete; see docs/PERSONAL_ASSEMBLY_AUDIT_20260910.md.

- 18:56 source05a7466 pushed: staff saved-design application with common renderer preview, permission/plan checks, CAS conflict and history; applied20260910135001 rollback-tested. Searchable stock nomenclature added.383tests/tsc/build74pass. Deployment pending at checkpoint. Personal unrestricted builder and authenticated employee/owner E2E remain incomplete. Six CRM documentation sets compared; not all registry providers audited.

- 18:32 follow-up: previous reservation clarification 4828b14 is Vercel Ready. Staff product creation with raster photos is implemented; new tenant-scoped RPC applied and rollback-tested (hidden product, stock movement, replay, missing permission/revocation). Source checks: 376 tests, TypeScript and production build pass. Actual staff upload/login browser acceptance is pending a separate employee email; broader personal assembly/payment/device acceptance is not complete.

- 17:08 staff/customer follow-up (source release pending): separate staff membership, owner invitation/revision/revocation RPCs, verified-email acceptance, employee orders/catalog/stock/customers/analytics and shared AI conversation implemented. Five migrations applied. Rolled-back fixtures verify owner/outsider/email/replay/revocation, product edits and stock movements. Local authenticated owner `/admin/team` and first-product Studio render verified. Buyer signed-cookie order history/status polling and reservation tracking implemented; real checkout→seller→buyer browser transaction remains unverified. This is not a full-product acceptance declaration.

- CRM/POS integrations: the owner catalog covers all 23 outreach targets plus 1C/other and stores independent per-provider requests. Production migration `20260910111537` is applied with RLS retained. Planfix login and seven-scope consent are complete; deployed commits `c208e1c` through `d10b41e` harden the apex/`www` OAuth cookie and safely classify token failures. The callback reaches Planfix's token endpoint, but the trial account receives a non-JSON response and Planfix displays that API access requires a paid or Premium subscription, so no token/order exists; a no-secret diagnostic was sent to Planfix support on 2026-09-10. The Business.Ru test company `w833379` is registered and its private API integration `Dukenim` is saved. Connector commit `3b1b25c` and production migration `20260910210037` are deployed; the protected action performs signed token repair, a read-only `customerorders?help=1` preflight and encrypted tenant-scoped storage. Credential transfer is still incomplete because browser control stopped on an uncertain Edge URL; a production database check confirmed no `biznes_ru` row and therefore no live request/order/customer data. iiko explicitly permitted an open-API pilot but issued no `apiLogin`. The no-payment r_keeper demo/license request was sent to the official Kazakhstan dealer; no cost or contract was accepted.
- Composition6d07d41 is published/Ready33AdrUPENuSf1caWXToPRxeqJ9Z2;36production purchase journeys pass. Selected-page PDF visual analysis and four-step first-product form published47da9d8/ReadyPbAFHL5HzBx2LEGDGESScJooER2K.331tests/tsc/build pass, local browser checks and live Kimi page test pass. Full owner persistence acceptance is not replaced by mock-action UI tests. See newest AI_HANDOFF checkpoint.

- Production ea0b670: independent reservation settings, stock holds, confirmation/collection/cancellation and expiry cron; database isolation/idempotency/stock checks passed. Authenticated owner settings read verified; full reservation browser transaction not yet verified.
- Published:18sphere/approach compositions, six distinct editorial covers and coordinated product-page layouts; all36desktop/mobile purchase journeys pass, including compact mobile filters. Logo/PDF materials are available inside the initial design stage; selected-page visual analysis requires explicit send/review/save.
- Kimi-K2.6 real conversation, PNG vision and requested pink/green design/sections tests all pass. Resource, quota and billing unchanged.
- Higgsfield preflight:5credits available,2 per2K image. New bulk product photography not submitted; no purchase or generation charge. Existing demo images retained. Full additional-media set needs a sufficient balance.
- Full authenticated merchant creation/save/relogin, unrestricted individual layouts/fonts, merchant acquiring and native-device acceptance are not complete. PDF visual interpretation is implemented for one selected page per request, not automatic whole-document analysis. Amira remains unchanged until the owner's full-product gate. Native app/TestFlight paused by owner.

## Dated release history (not current acceptance)

- 2026-09-10 04:15: ea0b670 reservation+Planfix source published, Vercel success ECmC1mhcubQPvxTUWWR9TVHNyz5C and authenticated reservation settings read confirmed. Local composition follow-up implements6editorialcovers plus18sphere/approach layouts and mobile compact filters;317tests/tsc/build pass and3explicit real Kimi tests pass. Final browser regression running. No Azure/quota/billing change; no Amira seed. Full owner E2E/acquiring/native acceptance remain open.

- 2026-09-10 04:02 LOCAL SOURCE + applied DB: real opt-in reservation holds, expiry/cancel/collection and notification classification implemented. Five migrations applied; transactional isolation/replay/stock tests and existing colour/cost/fulfilment regressions pass, minutely expiry cron succeeded.310tests+additional API test,tsc/build and36localdemojourneys pass. No reservation source deployment yet; owner reservation UI E2E/device delivery unverified. Amira unmodified; full builder/18bespoke/acquiring still open.

- 2026-09-09 18:57: follow-up a4faf56 deployment Ready (dpl_3VdEYL3tEuQysAi1oXST3xh1ztjz); unpublished-store navigation uses protected preview.287tests/3skip,tsc/local+hosted build pass. This does not close reservations, full bespoke18compositions or acquiring; Amira unmodified.

- 2026-09-09 18:55: 85bf78e verified Ready (dpl_58ZTVyhhEpsdZk4PYU3kjBjrvsKD), canonical www/apex aliases. Custom shades, actual AI categories, receiving/payment-preference steps and explicit new-store publication released. 285tests/3skip, tsc/build, live Kimi exact-colours/sections and rollback isolation/creation tests pass;36production demo purchase journeys pass. Fresh authenticated Amira Studio opens single start CTA; full owner save/relogin remains untested. Follow-up private-preview navigation fix under verification. No Amira seed. Reservation/full18bespoke compositions/acquiring are not complete.

- 2026-09-09 18:38 IN PROGRESS: custom-theme, receiving/payment setup and explicit new-store publication migrations applied; rollback DB tests pass. Local UI/prompt not deployed. Kimi colour test passed before final prompt cleanup; browser flow unverified. Acquiring research reopened, FreedomPay candidate only. Amira not seeded; reservations/full18bespoke compositions remain open.

- 2026-09-09 17:51: 5085ec4 Ready on canonical domain (dpl_7RB8ehDGxJGZ7nxYYEUZvZ4pYF4R). Separate18demo route spaces with retained cart/layout; private cost snapshot migration applied, stock cost entry and gross profit released.272tests/2skip,tsc/build,36local journeys,transaction owner/outsider/immutability tests passed. Owner browser analytics empty-state verified; stock UI save not verified (current shop has no products). Acquiring paused. Individual shades/full builder/reservations remain unfinished.

- 2026-09-09 17:27: 1637906 published Ready (dpl_41vQDGad3pUXzAevPtH5BAoQ81PG), canonical domain verified. Segment-first demo chooser, add/buy-now dialog and distinct approach grids. Production purchase navigation passes at 390/1440 with cart quantity preserved. 267 tests pass/2 skip, tsc/build and 36 local layout checks pass. No live payment or authenticated AI-save acceptance; broader unfinished requirements remain open.

- 2026-09-09 16:53: bd30165 Ready dpl_7P8cRBaBFtv6sSsk8PShPUeMqaGr on canonical domain;18composed landing examples/3approaches and matching AI context published.267tests,36local viewport checks, demo navigation passed. Full merchant generation/save and remaining colour/commerce requirements not verified complete.

- 2026-09-09 16:50 source:18 composed landing examples and3 selectable approaches per sphere/plan, shared renderer and AI configuration context. Atomic creation migration applied with role/entitlement guards retained.267tests and36browser checks passed; full saved creation/live AI not yet verified. Not a claim of18complete independent storefront journeys or remaining commerce acceptance.

- 2026-09-09 16:40: d61b595 published, Vercel dpl_64uAAWZXftjx1UWoGfJM8kbF9bPc Ready on www/apex. Includes accumulated local navigation, PDF text, catalog filters, analytics, preview fixes and separate free-text colour-preference substage.248tests pass/2skip,tsc/build pass. This is not full individual colour generation,18templates,full builder,cost/reservation/acquiring completion.

- 2026-09-09 16:25 LOCAL: final setup preview separates demo products from real merchant products using the shared storefront renderer; CTA color override corrected. No sample data writes or production deployment. 13 focused tests passed; broader open acceptance items unchanged.

- 2026-09-09 16:08 LOCAL: separate brief/examples substage persisted in existing draft; initial palette grid removed,2illustrated existing choices per sphere using labelled demo assets. Six-field business guidance added to AI prompt; no Azure live verification.241tests pass/2skip,tsc clean; not18templates or full creation path. No deploy.

- 2026-09-09 15:07 LOCAL: shared public/preview catalog search/category/size/availability/price sorting, accurate same-variant stock match; removed invented Hit/New badges, real old-price/availability display.8focused tests,tsc/build passed; no browser interaction proof or deploy. Research expanded to exact PDP/category sources;18configurations and full site acceptance still open.

- 2026-09-09 afternoon LOCAL ONLY: initial Studio one CTA, collapsed steps saved before navigation; monochrome mobile4-item nav/More with secondary routes and official D mark; approved PDF.js6.3.289 text import/worker cleanup. Analytics corrected from last100/all-status misleading total to paid-order1/7/30-day cohort, paginated with tenant predicate. Payment guide/support intake added, no goods acquiring connected.226tests pass/2skip, tsc/build clean. Public Supabase local config recovered; private UI now correctly redirects to login, owner browser login requested; no visual/E2E proof. No deploy; published remains df287b7. App/TestFlight paused. Research18references preliminary;18working configurations, historical costs/profit, full builder/booking/prepayment still not complete. Polar SaaS eligibility unresolved.

- 2026-09-09 13:18: df287b7 Ready dpl_AZYugoNJySA4vWoQjSViKPWuzub6: unified light-grey Studio chat surface, white cards/composer and responsive spacing. Four Studio tests, tsc/build pass; visual owner review pending. EAS authenticated listing has no iOS builds. PDF/autobuilder/reservations/payment/native acceptance unchanged.

- 2026-09-09 03:06: b309465 Ready dpl_5Hp25xKWtG9RJXNV76rzZguPMupu; real category names published. Minutely queue-aware push cron recorded successful execution; signed endpoint200/anonymous401. Native delivery not tested (zero registered devices). Final214 tests/tsc/build pass; no new advisor findings. Full acceptance remains open, see checklist.

- 2026-09-09: bf3f8fc Ready dpl_AMdNXTGJLordZgbEQ7tJLqXL31f2. Supabase minutely push wake activated after signed live200/anonymous401. Queue-aware idle check avoids needless HTTP calls; short-lived HMAC keeps persistent key out of managed net tables. Native route code passes iOS JS export, no IPA/registered device/delivery proof. Category mapping follow-up fixes flattened real catalog sections; tsc/build and214 tests pass.

- 2026-09-09: 319a57e Ready dpl_9KYMHwtnB6jwqHKcVdpxyhj4vvdW: atomic AI proposal creation and cancellation stock return published. Mobile push deep-link/read/login-return source implemented; mobile tsc/lint/iOS JS export pass, no IPA/device test. Supabase queue-aware scheduler installed but inactive pending endpoint smoke; production/Vault secret configured. Full PDF, personalized assembly, merchant prepayment and reservations remain open.

- 2026-09-09: 37305d0 published Ready, dpl_BJjfgCTCWZUiH6ztcMCKCaAfSkDy. Kimi JSON/non-thinking request controls pass live text+PNG tests; private saved-logo analysis, brand context in generators, proposed custom accent/contrast, support wording confirmed in fresh owner browser. Push receipt/lease migration applied and transaction-tested; no production CRON_SECRET/scheduler/device delivery. Vercel Hobby verified. Follow-up atomic AI-design creation and cancellation stock-return migrations applied/tested; code release pending. Full PDF/personal assembly/prepayment/reservations remain open.

- 2026-09-09: Domain-support a4500ab published Ready (dpl_Dy1B8kucswwHFptKQhsTwc2ayW7i): automatic request on deliberate help-button POST, active-thread reuse, linked message history/replies for owner and platform admin. Migration applied and DB transaction tests passed; full browser flow not verified. Does not connect DNS automatically or deliver email/push. Existing Azure Kimi consultation passed a fresh live test in 8.1s; prior UI failure cause still unknown.

## Product direction and verified capabilities

- Studio da66579 Ready: single conversational workspace confirmed in fresh authenticated browser, inline wizard and preview, persisted draft reload fix. Design undo DB/RPC implemented and transaction-tested (latest-state CAS/replay rejection); browser undo unverified. Live corrupted CTA default and exact bad labels repaired. Full personalized builder/payment/reservation/push acceptance remains open.

- Security c8b66fa published Ready (dpl_ByxHemY2WobGsLsbf9xFzvxDPMX4): removed config-missing demo privilege fallback and non-Vercel middleware bypass; private route no-store, dynamic cabinets and preview protection. 164 tests, tsc/build passed; production dependency audit zero known advisories. Eight live anonymous/spoof-header requests require login with no-store; rolled-back DB owner role-escalation negative test passed. Not a full security certification; no change to outstanding builder/payment/reservation scope.

- Latest code 55c2c0e Ready (dpl_H6RXdHSv3UCTdN65V2zxmuZC6KR8), including owner-scoped AI proposal preview through shared renderer and corrected mobile queue. Still not full brief acceptance. No authenticated merchant E2E/native device confirmation; root has no personal tenant as requested.

- Release d70f7fd Ready on canonical domain (dpl_EhLSarKo9knaPV47BPWoBsb9Lcch): private brand logo/notes, pickup location/maps, immutable fulfilment snapshots, shared StoreHome preview, atomic catalog creation. 145 tests/tsc/build and rolled-back order/tenant checks pass; full authenticated-browser flow unverified. No new prepaid/reservation capability.

- Latest release 41af99a: persisted consultation in owner AI Studio, eight-pair model context, thirty-pair UI restore, typed task handoff into editors. Vercel dpl_DdNY7agEfS7W2K4Lk6FccgpB2MVN Ready on www/apex. 128 tests/tsc/build passed; explicit live Kimi conversation test passed. Database history read/isolation verified, full browser relogin and shop-building E2E not verified. Personalized brandbook builder still incomplete.

- Current release e62ad83: Vercel dpl_7B7V4QUN3fK6k9fb6yAtHLb3Efhs Ready on www/apex. 123 tests, tsc, build (62 routes) pass. Private builder-draft schema applied; API and form add explicit save/resume with revision conflict checks. DB persistence/isolation checked in rolled-back transaction; authenticated browser E2E unverified. Delivery enable/min_order controls also added. This is not full conversational builder completion.

- Publication verified: 2fd6815 Git deployment dpl_Brgey53BXZfTxZpCDK1KESyn1ppE Ready, canonical www/apex aliases. Delivery-zone editor and prior checkout fixes now deployed; authenticated save E2E remains unverified. Supersedes pending deployment statement below.

- Source 2fd6815: owner delivery-zone editor and checkout reliability fixes published through Git integration. 106 tests, tsc, build pass. Real authenticated zone save/reload still requires verification.

- Direction: AI-led personalized catalog creation and ongoing editing, optional logo/brandbook, shared draft/public renderer. This complete flow is NOT implemented; current AI proposes bounded text/structure/theme drafts.
- Platform identity: monochrome D/wordmark, grey threshold/dot. Tenant brands remain individual. See docs/BRAND_CURRENT.md; old gold PDF/icons/video are not updated masters.
- Public plans: Start and Brand, AI in both within entitlements. No pricing change in current work.
- Launch scope: goods and ready-made food. No service booking, event tickets or ingredient warehouse.
- Fulfilment: courier/pickup with payment on receipt and opt-in unpaid reservation stock holds are live. Reservation expiry/cancel/collection database checks pass; full owner browser roundtrip remains unverified. Prepaid pickup is not active without a verified goods-payment provider.
- Mobile: EAS linked and App Store signing ready. No IPA/TestFlight release. Push infrastructure exists in source, but production delivery/device E2E is unverified. Notifications must distinguish fulfilment types and payment state; tapping opens the permitted order/reservation.
- Superadmin remains independent of a personal store. Main owner's unused store was deleted; account retained.
- Acceptance ledger: docs/ACCEPTANCE_CHECKLIST.md. Remaining builder and fulfilment requirements are in progress, not completed by the zone release.
- Azure management works; Kimi-K2.6 live consultation test passed. Later GET now verifies NoAutoUpgrade, current Free Tier; prior inconsistency resolved. No further owner login required. Free quota tier does not guarantee free inference or cap bills.
- Release gates: persisted builder/real preview, fulfilment and payment/hold transactions, authenticated commerce E2E, tenant negative tests, backups/restore, legal/payout checks, mobile device QA.

## Historical release notes (not a current readiness checklist)

2026-09-08 local only: checkout error handling preserves cart and releases pending on network/invalid responses; missing backend no longer invents shipping options. 95 tests, tsc and production build pass. Not deployed. Personalized conversational builder remains incomplete; audit and implementation sequence: docs/CONVERSATIONAL_COMMERCE_20260908.md.

History last consolidated: 2026-09-08

2026-09-08 launch scope: onboarding now offers only goods/ready-made food, excluding service booking and event ticketing in UI and application API. Source 55283f9, deployment dpl_Cg6ZdC4uawBDeFNmUJP8eEjdP9Zq READY/aliased; 87 tests, TypeScript and build pass. Existing tenants and historical demo routes unchanged. Ingredient inventory remains unsupported and explained for food.

2026-09-08: c638e86 published on canonical domain (dpl_HzaXadNxP4ReJkpev6guMiG1EAgo READY). Main platform administrator no longer has a personal tenant, per owner request; account/role retained and deletion audited. Tenantless admin routes to root. Product creation inputs and quantity navigation now adapt to eight business verticals; true service scheduling, event ticketing and ingredient inventory remain unimplemented. 82 tests, tsc/build pass. See docs/BUSINESS_VERTICAL_AUDIT.md and docs/PRELAUNCH_AUDIT.md; no full authenticated commerce E2E claim.

Serik presentation release 55a6f37: dpl_GXd7mTehtyNnr7gLpo5PUYB7pGqR Ready/aliased. Demo names and person/business placeholders now Серик / Серик Шоп. Fresh phone and CRM recordings with new URLs; Brand card has restrained neon emphasis, pricing unchanged. Presentation smoke passes on canonical domain at 390/1440; both WebM assets serve successfully. 64 tests, tsc/build pass. Real first-run persistence E2E still requires isolated authenticated test account; existing owner store was not reset or renamed.

Catalog wizard release 38324c0: dpl_4zrrBjQq8TvNLt4YqKmAuNkCV14c READY and canonical aliases confirmed. First-run design step offers validated AI template/palette recommendations; competing separate composer removed. tsc, 64 tests and production build pass. Anonymous AI Studio redirects to login. Authenticated first-run generation/save E2E remains unverified. Detailed audit and execution prompt: docs/CATALOG_CREATION_AUDIT.md.

Media release confirmed: dpl_9fjZ6JaNRy1NG2VkpXxNZ4Axrksx READY, www.dukenim.kz and apex aliased. All seven niche media smoke checks passed on production at 390/1440; mask PNG returns 200. Source 62b9c10. This supersedes the in-progress media note below.

Media continuation source 62b9c10: every demo offering has an image; beauty/food five positions, other non-fashion demos four each. Generated 25 niche illustrations and one corrected featureless-mask still. No new video is complete. Higgsfield balance verified 5 credits, no top-up. Browser media/loading/overflow checks passed for all seven niches at 390 and 1440; tsc and 59 tests pass. Final Vercel release is being checked before reporting publication.

Latest demo release: dce2749; Vercel dpl_FrkMhX1eK2kFGpNVva5oJoi2JddJ reached READY and alias www.dukenim.kz. `/demo` offers eight business examples; fashion has ten products, black/white styling, separate catalog/category routes and return to Studio. Tests: 58 passing, tsc/build passing; local mobile/desktop navigation passed. Outstanding work is explicitly tracked in docs/REQUEST_AUDIT_20260907.md, including unfinished promo video and conversational builder.

## Update — 2026-09-07

- AI Studio can now produce a structured storefront-design proposal (tariff-allowed layout, curated palette and hero copy) and apply it only after the owner presses an explicit button. The apply route uses the signed-in Supabase session, RLS and tenant/intent filters, preserves the owner's existing image and brand colour, and rechecks plan access. Production migration `20260907110158_ai_store_design_intent.sql` is applied and verified. Release `bf65916` is deployed as Vercel `dpl_5aCPC5YChSoDxXFkRe5qgkTBM8SW` and aliased to `www.dukenim.kz`; 50 tests and the 58-route build pass.
- AI-generated hero copy can now be applied explicitly while preserving the current template, palette, image and brand colour. On «Бренд», generated promotion copy can be saved as an idempotent unpublished campaign draft. Release `81d06eb` is deployed as Vercel `dpl_5AHSM3XKDY8ZV1CqqoqM65hHQuHe`; 55 tests and the 59-route build pass.

- Release 95b38d1 is Ready on canonical domains; authenticated production UI check confirms new AI Studio. This verifies deployment/UI, not the full new-store write journey.

- AI-first first-run workflow is implemented in source: Studio available before first product, embedded catalog/product editors, real explicit saving of generated category names, mobile-primary AI navigation, and a domain help page. TypeScript, 45 tests and production build passed; production authenticated E2E remains required.
- Root plan/status mutation now validates input, records intent before mutation and updates both fields together. Full root catalog CRUD is still incomplete.
- The previous d49ea5e release is verified Ready on both canonical domains. The four Polar tariff checkout creations were verified in its release preparation; real settlement/payout is not verified. Higgsfield production is pending a spending ceiling, not completed.

## Update — 2026-09-06 (release not confirmed)

- Local homepage now uses the owner's monochrome direction. Full cabinet/mobile redesign remains incomplete; old gold/room website branding below is historical.
- Checkout v2, support context, onboarding preferences and AI campaign linking are implemented locally; six targeted 20260905 migrations were applied this session. Legacy anonymous order RPC was restricted. Matching source deployment/smoke remains a priority.
- Polar Dukenim products and production webhook exist; Vercel IDs/secret were changed. Access-token organization, old subscriptions and checkout remain unverified: migration is NOT complete. Backup, legal and native release gates remain open.
- The local public product story now has a reduced-motion-safe interactive transition from storefront phone to order to CRM (`CommerceMotion`). The official D symbol is reused as a monochrome mark in homepage/auth/admin surfaces. `/root/diagnostics` is a read-only superadmin health/audit page; root mutations reject local demo sessions.

## Product

Dukenim is a multi-tenant commerce platform for small and growing retailers in Kazakhstan. A customer uses a branded catalog/storefront while the owner manages products, orders, stock, customers, and analytics from `/admin`. Platform administration lives at `/root`.

## Technical foundation

- Next.js 15, React 19, strict TypeScript.
- Supabase with tenant isolation through RLS.
- Roles: `owner`, `staff`, `superadmin`, verified server-side.
- Prices are integer KZT values.
- Stock changes must go through `stock_movements`.
- Public storefront route: `/s/[slug]`.
- Polar checkout, signed subscription webhook and customer portal are implemented and production-configured with four KZT products in the legacy `dukenimkz` organization. The intended primary `Dukenim` organization now has its first approved monthly Start product (24 900 ₸); it is not wired into production checkout until all products, webhook and server credentials are migrated and tested together. The integration fails closed when configuration is incomplete and applies signed deliveries atomically. Polar payout/business onboarding still requires the owner's legal and bank details, so successful settlement must not be claimed yet.
- Google sign-in is live: the Google Cloud OAuth client has one fresh persistent secret stored only in Supabase Auth, the audience is in production, and a full production E2E completed from `/login` through Google consent to both `/root` and `/admin`. The old Google secret was disabled and permanently deleted in Google Cloud on 2026-09-04. Apple Developer membership, the Dukenim App ID and web Service ID are active and configured for the Supabase callback; Apple login remains disabled in production until the downloaded private key is securely supplied to Supabase/Vercel and the live E2E succeeds.
- Trial entitlement is enforced server-side: every trial has an explicit seven-day end, paid-tier actions use the effective `next_plan` during that window, public storefront RLS and server lookup reject expired trials, and AI API access uses the same entitlement decision.
- Public tariff source is now «Старт» 24 900 ₸/month or 239 000 ₸/year and «Бренд» 34 900 ₸/month or 335 000 ₸/year. The public selector is implemented locally; deployment still needs its normal release check.
- CRM request/queue infrastructure exists in production. A Central Asia Planfix test account and an account-scoped confidential OAuth pilot application now exist. Production commit `b2b9a56` has the PKCE connect/callback flow, server-only encrypted token storage and owner-triggered real-order export with atomic refresh-token rotation and no blind retries after uncertain delivery. Migration `20260910101627` adds a verified service-role-only provider-object/idempotency ledger. Vercel reported success and authenticated production navigation reached the Planfix login page. The live owner OAuth grant is still pending because Planfix login/password recovery must be completed privately. No customer order has been sent to Planfix. Azure Foundry is live in production through the server-only OpenAI-compatible client and the `Kimi-K2.6` deployment. The rotated key and endpoint/deployment configuration are stored only in Vercel Production; `/root/ai` and the tenant `/admin/ai-studio` generation flow were both verified end to end on 2026-09-03.

## Resilience infrastructure

- A private Backblaze B2 bucket for production backups exists with server-side encryption enabled; it is empty until a least-privilege application key and backup jobs are configured.
- Cloudflare Free zone onboarding for `dukenim.kz` is prepared with Vercel apex/`www` and mail DNS records. Public authoritative DNS remains at Hoster.kz until the registrar nameserver switch is completed and verified.
- GitLab is intended as a private source mirror; the mirror repository and first push are not yet complete.

## Product readiness

- Storefront, cart, checkout flow, owner dashboard, plans, onboarding, and root dashboard exist.
- Production Supabase now contains the application RPCs for onboarding, product creation, storefront orders, offline sales, and subscription activation. Checkout uses the database `courier`/`pickup` delivery values; real online payment remains intentionally unavailable until a provider is connected.
- Seven-day trial flow exists and does not require a bank card.
- Demonstration data exists; confirmed customer testimonials and commercial performance metrics do not.
- Legal templates (offer, privacy and cookies) exist but require real company details and legal review before commercial launch.
- Production has RLS-protected tables for promotion codes, tariff checkout requests, promotion redemptions, and root audit events. The public guest checkout RPC is still legacy-exposed until the server-side replacement is deployed and smoke-tested; its lockdown migration must not be applied earlier.
- `/admin/ai-studio` and `/admin/requests` (tenant-scoped `change_requests`/`messages`, already RLS-protected, with a root queue at `/root`) now cross-link each other as an explicit "ИИ-помощник / написать в поддержку" choice; no new ticket schema was needed.
- Storefronts have PWA manifests and a narrow service-worker shell that caches public static assets only. Admin, root, auth and API routes remain network-only to avoid retaining tenant or session data in browser storage. A native Expo SDK 57 iOS/Android client exists under `apps/mobile`: it uses the approved Dukenim identity, public Supabase Auth configuration, owner/superadmin role recognition, a native RLS-scoped order list and order detail, push/deep-link routing, notification settings and camera barcode scanning. EAS production build `90742768-52d6-4837-9608-0181d0e7977f` produced the first signed App Store IPA as iOS version 1.0.0 build 3 for `kz.dukenim.app`. TestFlight upload remains pending because the saved Apple CLI session expired and no App Store Connect API key is configured. Physical-iPhone sign-in, push, deep-link and camera acceptance remain release gates; native social OAuth is a separate completion step.
- Audit finding (2026-09-03): the historical local migration chain cannot be replayed cleanly because older 12-digit files reference types whose creation is not represented. Production was verified directly and contains the authoritative enum values. No ineffective late "fix" migration is retained; baseline/reconcile the old history before any blanket database push.

## Brand source of truth

- Current brandbook: `output/pdf/Dukenim_Brandbook_2026.pdf` (version 1.0, August 2026).
- Current production masters: `public/brand/dukenim-flat-*`.
- Core colors: Black Jade `#071B17`, Aged Gold `#B08A50`, Pale Stone `#F4F0E8`, Graphite `#101713`, Warm Sand `#E8DFD0`.
- The threshold and wordmark dot are Aged Gold in the current approved system.
- Font: Manrope.
- Older green-accent `dukenim-approved-*`, `dukenim-logo-combo*`, and older PDFs are historical, not the current source of truth.
- The shared application tokens and owner navigation now use the approved Black Jade/Aged Gold/Pale Stone system. Some feature-specific legacy CSS remains and should be removed incrementally when those screens are touched.

## Marketing system

- Strategy, 30-day calendar, first-week scripts, and access plan exist under `marketing/`.
- Root marketing dashboard, trend ingestion endpoint, Supabase migration, Vercel cron, and GitHub Actions trigger exist locally.
- Migration `202608130001_marketing_engine.sql` has not been confirmed as applied to production.
- Instagram and TikTok publishing accounts are not connected through OAuth.
- No automatic publication or paid content generation is authorized without explicit approval of the material and cost.

## Delivery state

Catalog lifecycle is now represented on each tenant as `not_started`, `building`, or `ready`; the corresponding migration is applied to the connected production Supabase project. The dashboard routes owners to explicit catalog creation before product creation.

Production release `c0b7753` is Ready on Vercel and aliased to `dukenim.kz`/`www.dukenim.kz`. Google OAuth and authenticated owner/root access are verified. The responsive admin navigation is verified at desktop and 390×844 mobile viewports, including the complete «Ещё» menu. Azure `Kimi-K2.6` passes the superadmin diagnostic and produces a schema-valid tenant AI Studio draft in production.

Production release `2e3066f` extends the monochrome public homepage with an inspected Higgsfield promo film and interactive niche storefront examples. The same niche-preset engine powers the live catalog preview inside AI Studio/catalog setup. Generated goods are explicitly labeled as demonstrations. Vercel deployment `dpl_7bQm9EK7A1z6wP5VsX8bGShedieZ` is Ready and aliased to `www.dukenim.kz`; TypeScript, 45 tests, the 56-route build and production smoke pass.
Production Supabase now enforces category/tenant ownership inside `create_product_with_variants`; migration `20260907073737_enforce_product_category_tenant.sql` is applied and verified.
Production commits `9048fc3` and `72b3847` add `/root/stores/[id]`: audited superadmin product corrections and reversible visibility controls, plus store-scoped orders/categories/audit context. It does not expose permanent deletion, credential access, arbitrary SQL or direct stock mutation. Vercel deployment `dpl_DjwNMU3yNgmQTPEqQYYYRLaTC1p5` is Ready and aliased; authenticated production visual QA passed. TypeScript, 47 tests and the 56-route build pass.
Production commits `9200072` and `61d5640` align saved storefront templates with public rendering, add vertical-specific setup language, restore the public `/s/demo-shop` route under production Supabase, and place exact Dukenim phone/owner recordings on the homepage. Demo checkout is explicitly non-persistent. Vercel deployment `dpl_D4RUetx1BczS37mPFUiYbEr7CRiM` is Ready and aliased to `dukenim.kz`/`www.dukenim.kz`; smoke checks pass for homepage, demo catalog/product/checkout, AI Studio redirect and both media assets. TypeScript, 46 tests and the 57-route build pass.
Production commits `6b13fd0` and `de19719` simplify AI Studio into one visible builder step at a time: three compact template choices render one large static illustration instead of embedded storefront iframes; mobile 390×844 fits the complete choice step without nested or horizontal scrolling; first-product upload controls and the bottom composer are visually separated. Vercel deployment `dpl_XGUS4uZHGvtcZn4SV5tqoTt1W2j4` is Ready and aliased to `www.dukenim.kz`; TypeScript, focused tests, 75-route build and authenticated production smoke pass.
Design references (local, 2026-09-04): hero now uses `public/design/dukenim-home-hero-reference-v6.png` with separate catalog, CRM and phone objects in one warm room. Blocks 02 and 03 were regenerated to continue the same room and palette; no deployment yet.
## Apple signing update — 2026-09-08

EAS production signing credentials now ready: distribution certificate and active App Store provisioning profile created. Capability sync succeeds after preserving Sign in with Apple in mobile config; Push Notifications enabled. No signed build or TestFlight upload yet. Native functional/device QA and production public environment remain release gates.

- 2026-09-19: food assortment template now includes full-screen photo stories, compact header, sticky category navigation, two-column mobile cards, quick add and cart total in a soft-purple/light theme. Commit 9f6b337 is locally committed and deployed directly; Vercel dpl_BZWHfcL9Doh49NwewCG3UgSAWcEq Ready/current. Production stories/search/cart at 390/1440 and delivery/scheduled-checkout at 390 pass. GitHub push awaits restored github.com:443 connectivity; no merchant data reset or template reassignment.

## Buyer ordering and storefront performance — 2026-09-21

Current production source includes `385de91` and `c93d0a9` on `main`, with Vercel Ready. When SMS is not configured, checkout accepts contact details and privacy consent without OTP; a confirmed Google/email account can own same-browser signed guest receipts and retain order history/loyalty across devices. Unverified phone numbers alone never grant access to history. SMS OTP and merchant Sender ID delivery remain unavailable until the owner connects a provider; this connection was deliberately deferred.

A real buyer-to-owner order was verified in a temporary isolated production food store at 390px. Buyer order #2 persisted after reload, and the authenticated owner saw orders #1/#2 with contact, address, manual Yandex courier instructions and goods-only total. The owner was returned to «Моё кафе» and the exact temporary tenant was deleted; zero fixture tenants/orders remain and both pre-existing stores remain. This does not verify Google login on the original device followed by retrieval from a second device, nor SMS delivery.

Store layout now avoids fetching the full product and variant catalog on navigation; food loads no header categories and other storefronts load only category names. Owner order data fetches run concurrently. The published `/s/dukenim-shop` passed 390/1440px headless Edge smoke at 200, zero page errors/overflow, TTFB 268/267 ms and DOMContentLoaded 1,783/1,678 ms in individual runs; no comparative benchmark or real-user metric exists. «Моё кафе» remains private and correctly returns 404 to public visitors. The mobile courier acknowledgement is above the cookie dialog and the complete order path passed after this fix. Azure resources, model usage, quotas and billing were unchanged.

## Sales-control release — 2026-09-22

Production `main` includes `0507667` and `ece28af`; the latter is Ready and aliased to `www.dukenim.kz`. Merchants can enable manual Kaspi Pay remote invoicing, set a payment link or send an invoice to the buyer's phone, then record payment only after checking their own Kaspi receipt. Buyers see the exact order amount, a two-step payment explanation, and payment/fulfilment status in My Orders. The database prevents an unpaid Kaspi order from being marked ready or handed over. This is manual payment coordination, not an acquiring gateway or automatic money verification. Root can record a manual cash refund with exact order/amount/reference/reason and audit; the actual transfer is outside Dukenim.

Root has a new management home with paid-order-only sales figures and a store list. General merchant support chat is separate from request threads, with Realtime subscriptions, read receipts and canned root replies. AI Studio offers a seven-day holiday prompt and a draft design/campaign workflow; a merchant reviews and applies drafts. Shared blue accents now include the application navigation and Dukenim D threshold/dot while the D letter remains black. The August brandbook's gold threshold is historical relative to the owner's newer decision in `DECISIONS.md`.

The three production migrations for manual Kaspi, request read receipts, and root cash refund plus one for general chat read receipts are applied. Rollback SQL checked role isolation, unpaid fulfilment denial, payment transitions, cash-refund exactness and read receipts without leaving QA orders/messages. Strict TypeScript, 472 standard tests (four live-AI tests skipped), production build, simulated 390px food flow, and authenticated root rendering pass. The root general chat subscribed to Realtime in production; a two-account conversation and real buyer-to-merchant Kaspi checkout remain unverified.

No existing accounts/stores/orders were deleted. A production inventory found 23 auth users, 20 stores and five orders, four marked paid; destructive cleanup awaits the owner's exact scope. SMS remains deferred. Global immediate session revocation, backup restoration, automatic payment refunds, provider-backed CRM, and physical-phone performance measurements remain unfinished. Supabase security/performance advisor findings persist; most `SECURITY DEFINER` endpoints are intentionally role-guarded but are not a blanket clean bill of security. Azure configuration, quota and spend were not changed.

Refund follow-up `b15c2eb` is Ready as Vercel `dpl_BBFwj4ZzNVG729uTWpCoMupV6nUM` and aliased to `www.dukenim.kz`. A recorded Kaspi refund now cancels an unfulfilled order so existing cancellation reversals run; the buyer sees «Оплата возвращена». Production rollback SQL confirmed the state transition and left no QA tenant. Strict TypeScript, focused tests, production build and public HTTP smoke passed. This still records a merchant-confirmed external refund rather than moving money through Dukenim.

Root account-control follow-up `e6a9cc3` is Ready as Vercel `dpl_8BSY41mgrTpSnqcKEnhBjEn5ubgH` and aliased to `www.dukenim.kz`. `/root/accounts` can block or restore **new sign-ins** for non-superadmin accounts after exact email/reason confirmation and an audit-intent write. The published root browser page shows the control on owner/customer rows and omits it on both superadmin rows. Five action tests, strict TypeScript and production build pass. No real account was blocked; existing access JWTs are not immediately revoked by this control.

Production release `cf40910` (Vercel `dpl_E5heWkuVd5hTwGBS3HtwBTw8BCnK`, Ready/Current) corrects simulated checkout: the final demo screen explicitly states no order, merchant notification or payment occurred, and the demo footer/final CTA lead to `/register`. Production browser verified the full synthetic checkout path and revised final screen; database counts stayed at 23 users, 20 stores, 5 orders and 17 Storage objects. Legacy purge still awaits independent DB and Storage backup plus an isolated restore; Supabase Dashboard login is unavailable in the current browser session. Azure remains unchanged.

## AI Studio and manual Kaspi buyer journey — 2026-09-22

Production `ab18db8` is Ready as Vercel `dpl_235QgzvUHbxmhKcTju3PD8JFofTz` and aliased to `www.dukenim.kz`. A disposable owner registered in the real browser, completed the food onboarding, created a product and published a shop. Authenticated AI Studio states before and after catalog creation rendered at 390×844 and 1440×900 without page overflow; its lower mobile actions now wrap instead of clipping. The first-run setup no longer labels the six-step food path as five steps, and delivery is marked complete only after real pickup/zone configuration.

On the published disposable shop, a guest placed a 4,200 ₸ pickup order with Kaspi Pay and found it in My Orders; the owner saw the same pending order and could not mark it assembled before payment. A second guest order used manual Yandex delivery: required full-screen consent, address, variable courier price acknowledgement and goods-only total were verified, and the owner saw the address and manual courier instructions. The public Yandex notice now uses a server-only settings read and correctly names Kaspi Pay; an earlier version briefly hid the notice from guests because `tenant_settings` is owner-only under RLS. The disposable owner, tenant, two orders and QA audit data were removed after testing; exact QA counts are zero. Other accounts and stores were preserved.

No real Kaspi transfer, merchant receipt or physical iPhone test was performed. The Kaspi flow remains manual: the owner sends a genuine invoice or supplies their merchant link, checks the actual receipt, then marks the order paid. Azure configuration, quota and usage were unchanged. Detailed acceptance: `docs/ACCEPTANCE_20260922_AI_STUDIO_KASPI.md`.
