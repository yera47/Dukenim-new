import Link from "next/link";
export function StockFilters({query,filter,shown,total}:{query:string;filter:string;shown:number;total:number}){
 return <><form method="get" className="card mt-5 flex flex-wrap items-end gap-3 p-4"><label className="min-w-48 flex-1 text-sm">Поиск товара<input type="search" name="q" defaultValue={query} maxLength={100} placeholder="Наименование, артикул, размер или цвет" className="input mt-1"/></label><label className="text-sm">Наличие<select name="filter" defaultValue={filter} className="input mt-1"><option value="all">Все позиции</option><option value="available">Есть в наличии</option><option value="low">Мало: менее 3 ед.</option><option value="empty">Закончились</option><option value="missing-sku">Без артикула</option></select></label><button className="btn btn-primary">Найти</button><Link href="/admin/stock" className="btn btn-secondary">Сбросить</Link></form><p className="mt-3 text-sm text-neutral-500">Показано {shown} из {total} вариантов. Доступно к продаже — количество после списаний и удержаний по заказам и броням, а не результат физической инвентаризации.</p></>;
}
