"use client";
import {useState} from "react";
export function PromotionValue(){const[type,setType]=useState("percent");const[value,setValue]=useState(20);return <div className="space-y-3"><select name="type" className="input text-black" value={type} onChange={e=>setType(e.target.value)}><option value="percent">Скидка, %</option><option value="fixed_kzt">Скидка, ₸</option><option value="free_days">Доп. дни доступа</option></select>{type==="percent"?<label className="grid gap-2 text-sm">Скидка: {value}%<input type="range" name="value" min={1} max={100} step={1} value={value} onChange={e=>setValue(Number(e.target.value))}/><div className="flex flex-wrap gap-2">{[5,10,15,20,30,50].map(n=><button type="button" key={n} aria-pressed={value===n} className="rounded-lg border px-3 py-2" onClick={()=>setValue(n)}>{n}%</button>)}</div></label>:type==="free_days"?<label>Дни<select name="value" className="input text-black">{[1,3,7,14,30,60,90,180,365].map(n=><option key={n} value={n}>{n} дн.</option>)}</select></label>:<label>Сумма, ₸<input name="value" type="number" min={1} step={1} required className="input text-black"/></label>}</div>;}
