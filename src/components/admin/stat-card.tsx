import type{LucideIcon}from"lucide-react";export function StatCard({label,value,note,icon:Icon}:{label:string;value:string;note:string;icon:LucideIcon}){return <div className="card p-5"><div className="flex items-start justify-between gap-3"><span className="data-label">{label}</span><Icon size={19} className="text-[var(--accent)]"/></div><strong className="tabular mt-8 block text-3xl">{value}</strong><small className="mt-2 block font-bold text-[var(--ink-60)]">{note}</small></div>}
