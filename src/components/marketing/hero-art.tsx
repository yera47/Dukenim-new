"use client";import Image from"next/image";import{useRef}from"react";
export function HeroArt(){const ref=useRef<HTMLDivElement>(null);function move(e:React.PointerEvent<HTMLDivElement>){const r=e.currentTarget.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;ref.current?.style.setProperty("--mx",`${x*10}px`);ref.current?.style.setProperty("--my",`${y*8}px`)}return <div ref={ref} onPointerMove={move} onPointerLeave={()=>{ref.current?.style.setProperty("--mx","0px");ref.current?.style.setProperty("--my","0px")}} className="hero-art"><Image src="/dukenim-steppe-tumar-hero.png" alt="Единая система Dukenim: сайт, мобильное приложение и CRM" fill priority sizes="(max-width: 900px) 100vw, 62vw" className="object-cover"/><div className="hero-signal hero-signal-a"/><div className="hero-signal hero-signal-b"/><div className="hero-float">САЙТ <span/> ПРИЛОЖЕНИЕ <span/> CRM</div></div>}
