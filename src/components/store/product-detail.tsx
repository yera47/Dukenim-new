"use client";
import{useState}from"react";import Link from"next/link";import{Check,ChevronLeft,ShoppingBag}from"lucide-react";import{money,type Product}from"@/lib/demo-data";import{useCart}from"./cart-provider";
export function ProductDetail({product,slug,deliveryPolicy,returnPolicy}:{product:Product;slug:string;deliveryPolicy:string|null;returnPolicy:string|null}){const available=product.variants.find(v=>v.stock>0);const[selected,setSelected]=useState(available?.id??"");const[added,setAdded]=useState(false);const{add}=useCart();const imageStyle=product.images?.[0]?{backgroundImage:`url(${product.images[0]})`,backgroundSize:"cover",backgroundPosition:"center"}:undefined;const variationLabel=product.variants.some(v=>v.size)?"Выберите размер":"Выберите вариант";return <main className="container py-8"><Link href={`/s/${slug}`} className="muted inline-flex items-center gap-1 text-sm"><ChevronLeft size={17}/> Назад в каталог</Link><div className="mt-7 grid gap-10 md:grid-cols-[1.1fr_.9fr]"><div className="grid grid-cols-2 gap-3"><div style={imageStyle} className="product-image col-span-2 aspect-[5/4] rounded-[28px]"/><div className="product-image aspect-square rounded-[20px] opacity-80"/><div className="product-image aspect-square rounded-[20px] opacity-60"/></div><div className="md:p-5"><h1 className="text-4xl font-semibold">{product.title}</h1><div className="mt-4 flex items-center gap-3"><b className="text-2xl">{money(product.price)}</b>{product.oldPrice&&<s className="muted">{money(product.oldPrice)}</s>}</div>{product.description&&<p className="muted mt-6 leading-7">{product.description}</p>}<div className="mt-8"><b>{variationLabel}</b><div className="mt-3 grid grid-cols-4 gap-2">{product.variants.map(v=><button type="button" disabled={v.stock===0} onClick={()=>setSelected(v.id)} key={v.id} className={`relative h-12 rounded-xl border font-bold ${selected===v.id?"border-[var(--tenant-accent)] bg-[var(--accent-soft)]":"border-slate-200"} disabled:cursor-not-allowed disabled:bg-slate-100 disabled:text-slate-400`}>{v.size??v.color??"Один вариант"}{v.stock===0&&<span className="absolute inset-x-2 top-1/2 h-px rotate-[-18deg] bg-slate-400"/>}</button>)}</div></div><button type="button" onClick={()=>{if(selected){add(product,selected);setAdded(true)}}} disabled={!selected} className="btn btn-cta mt-8 w-full disabled:cursor-not-allowed disabled:opacity-50"><ShoppingBag size={19}/>{added?<><Check size={18}/> Добавлено</>:"Добавить в корзину"}</button>{(deliveryPolicy||returnPolicy)&&<div className="mt-7 space-y-4 border-t pt-6 text-sm">{deliveryPolicy&&<p><span className="font-bold">Доставка</span><span className="mt-1 block whitespace-pre-line opacity-70">{deliveryPolicy}</span></p>}{returnPolicy&&<p><span className="font-bold">Обмен и возврат</span><span className="mt-1 block whitespace-pre-line opacity-70">{returnPolicy}</span></p>}</div>}</div></div></main>}
