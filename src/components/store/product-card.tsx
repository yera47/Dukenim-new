import Link from"next/link";import{money,type Product}from"@/lib/demo-data";
export function ProductCard({product,slug}:{product:Product;slug:string}){const style=product.images?.[0]?{backgroundImage:`url(${product.images[0]})`,backgroundSize:"cover",backgroundPosition:"center"}:undefined;return <Link href={`/s/${slug}/product/${product.id}`} className="group"><div style={style} className="product-image aspect-[4/5] rounded-[var(--r-card)]"><span className="absolute left-4 top-4 z-10 rounded-full bg-white px-3 py-1 text-xs font-bold">{product.featured?"Хит":"Новая коллекция"}</span></div><div className="pt-4"><div className="flex justify-between gap-4"><h3 className="font-bold">{product.title}</h3><span className="whitespace-nowrap font-bold">{money(product.price)}</span></div><p className="muted mt-1 text-sm">{product.variants[0]?.color}</p></div></Link>}
