"use client";
import Link from"next/link";import{Menu,Search,ShoppingBag}from"lucide-react";import{useCart}from"./cart-provider";
export function StoreHeader({slug}:{slug:string}){const{count}=useCart();return <header className="sticky top-0 z-30 border-b border-black/5 bg-white/90 backdrop-blur"><div className="container flex h-18 items-center justify-between"><Link href={`/s/${slug}`} className="text-2xl font-black tracking-[.2em]">MEREY</Link><nav className="desktop-only flex gap-8 text-sm font-semibold"><Link href={`/s/${slug}`}>Новинки</Link><Link href={`/s/${slug}#catalog`}>Каталог</Link><Link href={`/s/${slug}#about`}>О бренде</Link></nav><div className="flex items-center gap-2"><button aria-label="Поиск" className="p-2"><Search size={20}/></button><Link aria-label="Корзина" className="relative p-2" href={`/s/${slug}/cart`}><ShoppingBag size={21}/>{count>0&&<span className="absolute right-0 top-0 grid size-5 place-items-center rounded-full bg-emerald-800 text-[10px] text-white">{count}</span>}</Link><button className="p-2 md:hidden" aria-label="Меню"><Menu size={22}/></button></div></div></header>}
