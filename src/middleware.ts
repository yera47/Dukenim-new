import{createServerClient}from"@supabase/ssr";import{NextResponse,type NextRequest}from"next/server";
import{getProfileRole}from"@/lib/queries/auth";
export async function middleware(request:NextRequest){if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY)return NextResponse.next();let response=NextResponse.next({request});const supabase=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,{cookies:{getAll:()=>request.cookies.getAll(),setAll(values){values.forEach(({name,value})=>request.cookies.set(name,value));response=NextResponse.next({request});values.forEach(({name,value,options})=>response.cookies.set(name,value,options))}}});const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.redirect(new URL("/login",request.url));const{data:profile}=await getProfileRole(supabase,user.id);if(request.nextUrl.pathname.startsWith("/root")&&profile?.role!=="superadmin")return NextResponse.redirect(new URL("/admin",request.url));if(request.nextUrl.pathname.startsWith("/admin")&&!['owner','superadmin'].includes(profile?.role??''))return NextResponse.redirect(new URL("/login",request.url));return response}
export const config={matcher:["/admin/:path*","/root/:path*"]};
