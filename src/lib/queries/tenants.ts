import type{SupabaseClient}from"@supabase/supabase-js";import type{Database}from"@/types/database";
export async function getPublicTenantBySlug(client:SupabaseClient<Database>,slug:string){const result=await client.from("tenants").select("id,slug,name,tagline,logo_url,accent_color,city,phone,whatsapp,instagram,plan,status,trial_ends_at,catalog_name,catalog_status").eq("slug",slug).in("status",["active","trial"]).maybeSingle();const tenant=result.data;if(tenant?.status==="trial"&&(!tenant.trial_ends_at||new Date(tenant.trial_ends_at).getTime()<=Date.now()))return{...result,data:null};return result}
