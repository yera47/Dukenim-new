import{createClient}from"@/lib/supabase/server";import{getTenant}from"@/lib/queries/owner";import{hasPlan,type Plan}from"@/lib/plans";export async function tenantHasPlan(tenantId:string,required:Plan){if(!process.env.NEXT_PUBLIC_SUPABASE_URL)return hasPlan("standard",required);const{data}=await getTenant(await createClient(),tenantId);return data?hasPlan(data.plan,required):false}
