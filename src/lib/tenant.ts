import{cache}from"react";import{createClient}from"@/lib/supabase/server";import{getPublicTenantBySlug}from"@/lib/queries/tenants";import{tenant as demoTenant}from"@/lib/demo-data";
export type PublicTenant={id:string;slug:string;name:string;tagline:string|null;logo_url:string|null;accent_color:string;city:string|null;phone:string|null;whatsapp:string|null;instagram:string|null;plan:"basic"|"standard"|"pro";status:"active"|"trial";trial_ends_at:string|null;catalog_name:string|null;catalog_status:"not_started"|"building"|"ready"};
export const resolveTenant=cache(async(slug:string):Promise<PublicTenant|null>=>{if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY){return slug===demoTenant.slug?{id:"10000000-0000-0000-0000-000000000001",slug:demoTenant.slug,name:demoTenant.name,tagline:demoTenant.tagline,logo_url:null,accent_color:demoTenant.accent,city:demoTenant.city,phone:demoTenant.phone,whatsapp:demoTenant.whatsapp,instagram:null,plan:demoTenant.plan,status:"active",trial_ends_at:null,catalog_name:demoTenant.name,catalog_status:"ready"}:null}const client=await createClient();const{data}=await getPublicTenantBySlug(client,slug);return data as PublicTenant|null});
