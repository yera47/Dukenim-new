import{NextResponse}from"next/server";import{requireRole}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";import{planPrice,type Plan}from"@/lib/plans";
export async function POST(request:Request){try{const{plan}=await request.json()as{plan?:Plan};if(!plan||!(plan in planPrice))return NextResponse.json({error:"Выберите доступный тариф."},{status:400});const{tenantId}=await requireRole(["owner","superadmin"]);if(!tenantId)return NextResponse.json({error:"Магазин не найден."},{status:400});if(process.env.NEXT_PUBLIC_SUPABASE_URL){const{error}=await(await createClient()).rpc("complete_onboarding",{p_tenant_id:tenantId,p_next_plan:plan});if(error)return NextResponse.json({error:error.message},{status:400})}return NextResponse.json({success:true})}catch{return NextResponse.json({error:"Не удалось завершить настройку."},{status:500})}}
