import Link from"next/link";import{Activity,BarChart3,Building2,CircleDollarSign,MessageSquare,Plus,Store,Users}from"lucide-react";import{createClient}from"@/lib/supabase/server";import{getPlatformMessages,getPlatformOrders,getPlatformRequests,getPlatformTenants,getSubscriptionPromotions}from"@/lib/queries/root";import{completeRequest,createPromotion,createStore,replyToOwner,togglePromotion,updateStore}from"./actions";import{money}from"@/lib/demo-data";import type{Database}from"@/types/database";
type Tenant=Database["public"]["Tables"]["tenants"]["Row"];type Order=Database["public"]["Tables"]["orders"]["Row"];type RequestRow=Database["public"]["Tables"]["change_requests"]["Row"];type Message=Database["public"]["Tables"]["messages"]["Row"];
export default async function Root({searchParams}:{searchParams:Promise<{tenant?:string}>}){let tenants:Tenant[]=[],orders:Order[]=[],requests:RequestRow[]=[],messages:Message[]=[],promotions:Database["public"]["Tables"]["subscription_promotions"]["Row"][]=[];const selected=(await searchParams).tenant;if(process.env.NEXT_PUBLIC_SUPABASE_URL){const client=await createClient();const[t,o,r,p]=await Promise.all([getPlatformTenants(client),getPlatformOrders(client),getPlatformRequests(client),getSubscriptionPromotions(client)]);tenants=t.data??[];orders=o.data??[];requests=r.data??[];promotions=p.data??[];if(selected)messages=(await getPlatformMessages(client,selected)).data??[]}const revenue=orders.filter(o=>o.status!=="cancelled").reduce((s,o)=>s+o.total,0);const metrics=[["МАГАЗИНЫ",tenants.length,Building2],["ОБЩАЯ ВЫРУЧКА",money(revenue),CircleDollarSign],["ЗАКАЗЫ",orders.length,Activity],["АКТИВНЫЕ",tenants.filter(t=>t.status==="active").length,Users]]as const;return <div className="min-h-screen bg-[#0c1713] text-white"><header className="border-b border-white/10"><div className="container flex h-20 items-center justify-between"><Link href="/root" className="flex items-center gap-3 text-xl font-extrabold"><span className="grid size-10 place-items-center rounded-[10px] bg-[var(--accent-bright)] text-[var(--accent-dark)]"><Store size={20}/></span>Dukenim Root</Link><span className="badge bg-white/9 text-[var(--accent-bright)]">SUPERADMIN</span></div></header><main className="container py-10"><div><div className="data-label text-white/38">DUKENIM.KZ · СОСТОЯНИЕ ПЛАТФОРМЫ</div><h1 className="mt-2 text-4xl font-extrabold md:text-5xl">Центр управления</h1></div><section className="mt-8 grid border-y border-white/10 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([t,v,I],i)=>{const Icon=I as typeof Store;return <div key={t} className={`p-5 ${i<3?"xl:border-r xl:border-white/10":""}`}><div className="flex items-center justify-between"><span className="data-label text-white/38">{t}</span><Icon size={19} className="text-[var(--accent-bright)]"/></div><b className="tabular mt-7 block text-3xl">{String(v)}</b></div>})}</section><div className="mt-7 grid gap-6 xl:grid-cols-[1fr_390px]"><section className="rounded-[14px] bg-white text-[var(--ink)]"><div className="flex justify-between border-b border-[var(--line)] p-6"><div><h2 className="text-xl font-extrabold">Все магазины</h2><p className="mt-1 text-sm text-[var(--ink-60)]">Тарифы, статусы и связь с владельцами</p></div><span className="badge">{tenants.length} ВСЕГО</span></div><div className="overflow-x-auto px-6"><table className="w-full min-w-[760px] text-left text-sm"><thead className="text-[var(--ink-60)]"><tr><th className="py-4">Магазин</th><th>Продажи</th><th>Управление</th><th>Связь</th></tr></thead><tbody>{tenants.map(t=><tr key={t.id} className="border-t"><td className="py-5"><b>{t.name}</b><small className="block text-[var(--ink-60)]">/s/{t.slug}</small></td><td className="tabular font-bold">{money(orders.filter(o=>o.tenant_id===t.id&&o.status!=="cancelled").reduce((s,o)=>s+o.total,0))}</td><td><form action={updateStore} className="grid gap-2"><input type="hidden" name="tenantId" value={t.id}/><div className="flex gap-2"><select name="plan" defaultValue={t.plan} className="rounded-[8px] border border-[var(--line)] bg-white p-2"><option value="basic">Старт</option><option value="standard">Бренд</option><option value="pro">Бренд (legacy)</option></select><select name="status" defaultValue={t.status} className="rounded-[8px] border border-[var(--line)] bg-white p-2"><option value="active">active</option><option value="paused">paused</option><option value="trial">trial</option></select></div><div className="flex gap-2"><input name="reason" required minLength={3} className="w-full rounded-[8px] border border-[var(--line)] px-2 text-xs" placeholder="Причина изменения"/><button aria-label="Сохранить" className="font-extrabold text-[var(--accent)]">OK</button></div></form></td><td><Link href={`/root?tenant=${t.id}`} className="font-extrabold text-[var(--accent)]">Открыть →</Link></td></tr>)}</tbody></table></div></section><aside className="rounded-[14px] bg-white/7 p-6"><div className="flex items-center gap-3"><Plus className="text-[var(--accent-bright)]"/><h2 className="text-xl font-extrabold">Новый магазин</h2></div><p className="mt-2 text-sm leading-6 text-white/48">Создайте пространство и сразу привяжите владельца.</p><form action={createStore} className="mt-5 space-y-3"><input name="name" required className="input text-black" placeholder="Название"/><input name="slug" required className="input text-black" placeholder="slug-магазина"/><input name="email" type="email" required className="input text-black" placeholder="Email владельца"/><input name="password" type="text" required minLength={8} className="input text-black" placeholder="Временный пароль"/><div className="grid grid-cols-2 gap-2"><input aria-label="Цвет магазина" name="color" type="color" defaultValue="#16a36a" className="h-12 w-full rounded-[10px] bg-white p-1"/><select name="plan" className="input text-black"><option value="basic">Старт</option><option value="standard">Бренд</option></select></div><button className="btn w-full bg-[var(--accent-bright)] text-[var(--accent-dark)]">Создать магазин</button></form></aside></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><section className="rounded-[14px] bg-white/7 p-6"><div className="flex items-center gap-3"><MessageSquare className="text-[var(--accent-bright)]"/><h2 className="text-xl font-extrabold">Заявки владельцев</h2></div>{requests.length?requests.map(r=><div key={r.id} className="mt-4 border-t border-white/10 pt-4"><p>{r.text}</p><div className="mt-3 flex justify-between text-xs text-white/42"><span>{tenants.find(t=>t.id===r.tenant_id)?.name}</span>{r.status!=="done"&&<form action={completeRequest}><input type="hidden" name="requestId" value={r.id}/><button className="font-bold text-[var(--accent-bright)]">Отметить готовой</button></form>}</div></div>):<p className="mt-5 text-white/45">Новых заявок нет.</p>}</section><section className="rounded-[14px] bg-white/7 p-6"><h2 className="text-xl font-extrabold">Чат с владельцем</h2>{selected?<><div className="mt-4 h-56 space-y-3 overflow-auto rounded-[10px] bg-black/18 p-3">{messages.map(m=><div key={m.id} className={`max-w-[80%] rounded-[10px] p-3 text-sm ${m.from_role==="superadmin"?"ml-auto bg-[var(--accent)]":"bg-white/10"}`}>{m.text}</div>)}</div><form action={replyToOwner} className="mt-3 flex gap-2"><input type="hidden" name="tenantId" value={selected}/><input name="text" required className="input text-black" placeholder="Ответ владельцу"/><button className="btn bg-[var(--accent-bright)] text-black">→</button></form></>:<p className="mt-5 text-white/45">Выберите магазин в таблице.</p>}</section></div><section className="mt-6 grid gap-6 xl:grid-cols-[.9fr_1.1fr]"><article className="rounded-[14px] bg-white/7 p-6"><h2 className="text-xl font-extrabold">Новый промокод</h2><p className="mt-2 text-sm text-white/48">Код всегда проверяется на сервере и не суммируется с другими.</p><form action={createPromotion} className="mt-5 grid gap-3"><input name="code" required minLength={3} maxLength={32} className="input text-black" placeholder="BRAND30"/><input name="title" required className="input text-black" placeholder="Название кампании"/><div className="grid grid-cols-2 gap-2"><select name="type" className="input text-black"><option value="percent">Скидка, %</option><option value="fixed_kzt">Скидка, ₸</option><option value="free_days">Доп. дни доступа</option></select><input name="value" type="number" min={1} required className="input text-black" placeholder="30"/></div><div className="grid grid-cols-2 gap-2"><select name="plan" className="input text-black"><option value="">Оба тарифа</option><option value="basic">Старт</option><option value="standard">Бренд</option></select><input name="maxRedemptions" type="number" min={1} className="input text-black" placeholder="Лимит, необязательно"/></div><button className="btn bg-[var(--accent-bright)] text-[var(--accent-dark)]">Создать промокод</button></form></article><article className="rounded-[14px] bg-white/7 p-6"><h2 className="text-xl font-extrabold">Активные и прошлые коды</h2><div className="mt-4 space-y-3">{promotions.length?promotions.map(p=><div key={p.id} className="flex items-center justify-between gap-4 border-t border-white/10 pt-3"><div><b>{p.code}</b><p className="text-sm text-white/55">{p.title} · {p.discount_type === "percent" ? `${p.discount_value}%` : p.discount_type === "fixed_kzt" ? `${p.discount_value.toLocaleString("ru-KZ")} ₸` : `${p.discount_value} дн.`}</p></div><form action={togglePromotion}><input type="hidden" name="promotionId" value={p.id}/><input type="hidden" name="active" value={String(!p.is_active)}/><button className="text-sm font-bold text-[var(--accent-bright)]">{p.is_active?"Отключить":"Включить"}</button></form></div>):<p className="text-sm text-white/45">Промокодов пока нет.</p>}</div></article></section><section className="mt-6 rounded-[14px] bg-white/7 p-6"><div className="flex items-center gap-3"><BarChart3 className="text-[var(--accent-bright)]"/><h2 className="text-xl font-extrabold">Продажи платформы</h2></div><div className="mt-8 flex h-40 items-end gap-3">{tenants.slice(0,12).map(t=>{const amount=orders.filter(o=>o.tenant_id===t.id).reduce((s,o)=>s+o.total,0);return <div key={t.id} title={`${t.name}: ${money(amount)}`} className="flex-1 bg-[var(--accent-bright)]/75" style={{height:`${Math.max(6,Math.min(100,amount/Math.max(revenue,1)*300))}%`}}/>})}</div></section></main></div>}
