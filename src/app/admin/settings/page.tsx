import{requireRole}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";import{getTenant}from"@/lib/queries/owner";import{saveStoreSettings}from"@/app/admin/actions";
export default async function Settings(){const{tenantId}=await requireRole(["owner","superadmin"]);const tenant=!process.env.NEXT_PUBLIC_SUPABASE_URL?{name:"MEREY",tagline:"Вещи, которые остаются с вами",city:"Кызылорда",phone:"+7 777 000 00 00",accent_color:"#0E5C4A"}:(await getTenant(await createClient(),tenantId!)).data;if(!tenant)return null;return <><h1 className="text-3xl font-semibold">Настройки магазина</h1><form action={saveStoreSettings} className="card mt-6 max-w-3xl space-y-6 p-6"><div className="grid gap-4 md:grid-cols-2"><label className="text-sm font-bold">Название<input name="name" className="input mt-2" defaultValue={tenant.name}/></label><label className="text-sm font-bold">Город<input name="city" className="input mt-2" defaultValue={tenant.city??""}/></label></div><label className="block text-sm font-bold">Слоган<input name="tagline" className="input mt-2" defaultValue={tenant.tagline??""}/></label><label className="block text-sm font-bold">Телефон<input name="phone" className="input mt-2" defaultValue={tenant.phone??""}/></label><label className="block text-sm font-bold">Фирменный цвет<input name="accentColor" className="ml-4 h-10 w-20 align-middle" type="color" defaultValue={tenant.accent_color}/></label><p className="muted text-sm">Платёжные секреты недоступны браузеру и настраиваются только на сервере.</p><button className="btn btn-primary">Сохранить настройки</button></form></>}
