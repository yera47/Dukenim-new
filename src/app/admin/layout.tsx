import{AdminShell}from"@/components/admin/admin-shell";import{requireRole}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";import{getTenant}from"@/lib/queries/owner";export default async function Layout({children}:{children:React.ReactNode}){const{tenantId}=await requireRole(["owner","superadmin"]);const tenant=!process.env.NEXT_PUBLIC_SUPABASE_URL?{name:"MEREY",slug:"demo-shop",plan:"standard"}:(await getTenant(await createClient(),tenantId!)).data;if(!tenant)return null;return <AdminShell tenant={{name:tenant.name,slug:tenant.slug,plan:tenant.plan}}>{children}</AdminShell>}
