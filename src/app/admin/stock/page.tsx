import{requireRole}from"@/lib/auth";import{loadOwnerCatalog}from"@/lib/owner-data";import{offlineSale}from"@/app/admin/actions";import{StockMovementForm}from"@/components/admin/stock-movement-form";
export default async function Stock(){const{tenantId}=await requireRole(["owner","superadmin"]);const{products,variants}=await loadOwnerCatalog(tenantId!);return <><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-semibold">Склад</h1><p className="muted mt-2">Каждое изменение попадает в журнал: продажа, поступление, списание или корректировка.</p></div><span className="rounded-full bg-[var(--surface-muted)] px-3 py-1.5 text-sm font-semibold">{variants.reduce((sum,item)=>sum+item.stock_qty,0)} ед. в наличии</span></div><div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[1040px] text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">Товар</th><th>Артикул</th><th>Вариант</th><th>Остаток</th><th>Продажа офлайн</th><th>Журнал склада</th></tr></thead><tbody>{variants.map(v=>{const product=products.find(p=>p.id===v.product_id);const lowStock=v.stock_qty>0&&v.stock_qty<3;return <tr key={v.id} className="border-t align-top"><td className="p-4 font-bold">{product?.title??"Товар без названия"}</td><td className="p-4 font-mono text-xs text-[var(--ink-60)]">{v.sku||"Не указан"}</td><td className="p-4">{[v.size,v.color].filter(Boolean).join(" · ")||"Один вариант"}</td><td className="p-4"><span className={v.stock_qty===0?"font-bold text-red-700":lowStock?"font-bold text-[var(--warning)]":"font-bold"}>{v.stock_qty} шт.</span>{v.stock_qty===0&&<small className="mt-1 block text-red-700">Нет в наличии</small>}{lowStock&&<small className="mt-1 block text-[var(--warning)]">Мало на складе</small>}</td><td className="p-4"><form action={offlineSale} className="flex gap-2"><input type="hidden" name="variantId" value={v.id}/><input name="qty" type="number" min={1} max={v.stock_qty} defaultValue={1} className="input h-10 w-16 px-2 text-center" aria-label="Количество проданного"/><button disabled={v.stock_qty===0} className="btn btn-secondary h-10 px-3 text-xs disabled:opacity-40">Продали</button></form></td><td className="p-4"><StockMovementForm variantId={v.id} stock={v.stock_qty}/></td></tr>})}</tbody></table></div></>}
