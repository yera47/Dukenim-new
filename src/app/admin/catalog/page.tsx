import Link from"next/link";import{Search}from"lucide-react";import{money}from"@/lib/demo-data";import{requireRole}from"@/lib/auth";import{loadOwnerCatalog}from"@/lib/owner-data";import{toggleProduct}from"@/app/admin/actions";
export default async function Catalog(){const{tenantId}=await requireRole(["owner","superadmin"]);const{products,variants}=await loadOwnerCatalog(tenantId!);return <><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="muted text-sm">Управление общей витриной</p><h1 className="mt-1 text-3xl font-semibold">Каталог</h1></div><Link href="/admin/catalog/new" className="btn btn-primary">+ Добавить товар</Link></div><div className="card mt-7 overflow-hidden"><div className="flex gap-3 border-b p-4"><div className="relative max-w-md flex-1"><Search className="absolute left-3 top-3 text-slate-400" size={18}/><input className="input pl-10" placeholder="Поиск по товарам"/></div></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="p-4">Товар</th><th>Цена</th><th>Остаток</th><th>На витрине</th><th/></tr></thead><tbody>{products.map(p=><tr key={p.id} className="border-t"><td className="p-4"><div className="flex items-center gap-3"><span style={p.images[0]?{backgroundImage:`url(${p.images[0]})`,backgroundSize:"cover"}:undefined} className="product-image size-14 rounded-xl"/><div><b>{p.title}</b><small className="muted block">{p.description?.slice(0,55)}</small></div></div></td><td className="font-bold">{money(p.price)}</td><td>{variants.filter(v=>v.product_id===p.id).reduce((a,b)=>a+b.stock_qty,0)} шт.</td><td><form action={toggleProduct}><input type="hidden" name="productId" value={p.id}/><input type="hidden" name="active" value={String(p.is_active)}/><button aria-label="На витрине" className={`h-7 w-12 rounded-full p-1 transition ${p.is_active?"bg-[var(--accent)]":"bg-slate-300"}`}><span className={`block size-5 rounded-full bg-white transition ${p.is_active?"translate-x-5":""}`}/></button></form></td><td><Link href={`/admin/catalog/${p.id}/edit`} className="text-sm font-bold text-[var(--accent)]">Изменить</Link></td></tr>)}</tbody></table></div></div></>}
