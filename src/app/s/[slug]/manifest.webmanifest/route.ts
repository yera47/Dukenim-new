import{NextResponse}from"next/server";import{resolveTenant}from"@/lib/tenant";
export async function GET(_:Request,{params}:{params:Promise<{slug:string}>}){const{slug}=await params;const tenant=await resolveTenant(slug);if(!tenant)return NextResponse.json({error:"Not found"},{status:404});return NextResponse.json({name:tenant.name,short_name:tenant.name,description:tenant.tagline??`Магазин ${tenant.name}`,start_url:`/s/${slug}`,scope:`/s/${slug}`,display:"standalone",background_color:"#FFFFFF",theme_color:tenant.accent_color,icons:[{src:tenant.logo_url??"/icon-192.png",sizes:tenant.logo_url?"any":"192x192",type:"image/png"},{src:"/icon-512.png",sizes:"512x512",type:"image/png"}]},{headers:{"Content-Type":"application/manifest+json","Cache-Control":"public, max-age=300"}})}
