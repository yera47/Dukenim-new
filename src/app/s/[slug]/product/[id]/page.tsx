import{notFound}from"next/navigation";import{ProductDetail}from"@/components/store/product-detail";import{resolveTenant}from"@/lib/tenant";import{loadProduct}from"@/lib/storefront-data";
export default async function ProductPage({params}:{params:Promise<{slug:string;id:string}>}){const{slug,id}=await params;const tenant=await resolveTenant(slug);if(!tenant)notFound();const product=await loadProduct(tenant.id,id);if(!product)notFound();return <ProductDetail product={product} slug={slug}/>}
